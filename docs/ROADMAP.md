# Meal Mate Roadmap

This is the durable operational source of truth after the MVP as well as during it. The PRD defines product intent; task cards define bounded work; this file alone records live task status, sequence, gates, decisions, blockers, and evidence. Status cells in task cards point here rather than duplicating this register.

## Current milestone

- **Milestone:** LOCAL-CORE-LOOP-READY; EMULATOR-PERSISTENCE-READY achieved 2026-08-29, DOMAIN-READY achieved 2026-08-27.
- **Current implementation task:** none — `MVP-017` reached `Done` 2026-09-03 (orchestrated); the implementation lane is clear.
- **Next implementation task:** `DEC-003` — `Ready`; selected by the orchestrator as the next card in `docs/task/SEQUENCE.txt`.
- **Concurrent owner-paced work:** `DEC-004` — `In Progress`; it does not occupy the implementation lane, but it still directly gates `MVP-018`, `MVP-021`, and `MVP-022` and transitively gates `MVP-019` and `MVP-020`.
- **Platform:** Android MVP and initial launch. iOS is the first post-launch platform priority.

## Status contract

`Draft → Ready → In Progress → Verify → Done`. A material change to a card's sources, decisions, product scope, architecture, or declared dependencies (its `Depends on` cell) returns an affected card to Draft. A package added inside a card's stated scope is recorded in the card's evidence and is not a Draft event. This sentence is the single Draft-trigger rule (D-031); `docs/task/README.md` points here. `Ready` means only that the card is eligible to plan: it is the selected next implementation task and every direct dependency is Done. Execution additionally requires a current approved plan against current sources. `In Progress` occupies the single implementation lane. `Verify` means implementation is complete but final evidence or owner status approval is unresolved. Move to `Done` only when all required evidence is recorded as `PASS` (or the D-027 owner-acceptance exception is fully satisfied) and the owner explicitly approves that card's status transition. Approvals remain nontransitive. The mandatory planning, execution, and handoff checks are defined once in `docs/task/README.md`.

## Delivery gates

| Gate | Exit condition |
|---|---|
| DOMAIN-READY | DEC-001, PRE-001, MVP-001, PRE-002, DEC-005, and MVP-002 Done — met 2026-08-27 |
| EMULATOR-PERSISTENCE-READY | DEC-002 Done and MVP-003 through MVP-005 Done with on-device SQLite persistence evidence (re-derived after DEC-005, D-029). **Closed 2026-08-29:** DEC-002, MVP-003 and MVP-004 are Done with on-device evidence; MVP-005 reached `Done` 2026-08-29 with independent-verifier PASS |
| LOCAL-CORE-LOOP-READY | MVP-006 through MVP-017 and MVP-023 through MVP-025 Done, including offline recovery. **Progress (2026-08-29):** MVP-006, MVP-007, MVP-008, MVP-009 and MVP-011 `Done` (MVP-011 AC-3 `NOT VERIFIED` under D-027, owner-accepted 2026-08-29); MVP-010 `Cut`; MVP-012 `Done` 2026-08-29; MVP-014 `Done` 2026-08-29; MVP-013 `Done` 2026-08-29; MVP-015 `Done` 2026-08-29; MVP-016 `Done` 2026-08-30; MVP-023 `Done` 2026-09-02; MVP-025 `Done` 2026-09-02; MVP-024 `Done` 2026-09-02; MVP-017 In Progress |
| REAL-DEV-BACKEND-READY | DEC-003, MVP-018, and MVP-019 Done in a non-production project |
| SHARING-SECURITY-READY | MVP-020 and MVP-021 security, projection, and routing evidence passes |
| PRODUCTION-BETA-READY | DEC-004 Done and MVP-022 passes; production activation remains a separately authorized action |

## Task register

| ID | Status | Outcome | Depends on |
|---|---|---|---|
| DEC-001 | Done | Product/platform identity (name deferred to DEC-004) | — |
| PRE-001 | Done | Android toolchain readiness | — |
| MVP-001 | Done | Clean-room Flutter scaffold | DEC-001, PRE-001 |
| PRE-002 | Done | Rust/FRB toolchain and bridge spike | MVP-001 |
| DEC-005 | Done | Bridge backend and core-language commitment | PRE-002 |
| DEC-002 | Done | Engineering-experience foundation | MVP-001 |
| MVP-002 | Done | Rust household kernel and SQLite foundation | PRE-002, DEC-005 |
| MVP-003 | Done | Android shell and navigation | MVP-002, DEC-002, DEC-005 |
| MVP-004 | Done | Anonymous-first entry and household identity | MVP-002, MVP-003 |
| MVP-005 | Done | Planning cycle and meal scope | MVP-002, MVP-004 |
| MVP-006 | Done | Minimal onboarding/preferences | MVP-003, MVP-004, MVP-005 |
| MVP-007 | Done | Ingredient and recipe foundation | MVP-002, MVP-004 |
| MVP-008 | Done | Manual recipe CRUD | MVP-003, MVP-007 |
| MVP-009 | Done | Restriction warnings/filtering | MVP-006, MVP-007, MVP-008 |
| MVP-010 | Cut | Recipe photos — cut 2026-08-29 (owner decision, SEQUENCE step 17, PRD v3 §16 photo deferral) | MVP-004, MVP-008 |
| MVP-011 | Done | Curated starter content | MVP-007, MVP-008, MVP-009 |
| MVP-012 | Done | Planned meals and components | MVP-004, MVP-005, MVP-007 |
| MVP-013 | Done | Planner experience | MVP-003, MVP-009, MVP-012 |
| MVP-014 | Done | Binary, optional pantry | MVP-003, MVP-004, MVP-007 |
| MVP-015 | Done | Conservative shopping aggregation | MVP-012, MVP-014 |
| MVP-016 | Done | Shopping-list experience | MVP-003, MVP-004, MVP-015 |
| MVP-017 | Done | Offline durability and recovery | MVP-008, MVP-009, MVP-013, MVP-014, MVP-016 |
| DEC-003 | Ready | Cloud/sharing release boundary | MVP-017 |
| DEC-004 | In Progress | Naming clearance and production identity | DEC-001 |
| MVP-018 | Draft | Real dev backend and durable auth | MVP-017, DEC-003, DEC-004 |
| MVP-019 | Draft | Privacy-safe observability | MVP-018 |
| MVP-020 | Draft | Share publication and web preview | MVP-009, MVP-018, DEC-003 |
| MVP-021 | Draft | Android App Links recipient flow | MVP-020, DEC-004 |
| MVP-022 | Draft | Android beta readiness | DEC-004, MVP-001–MVP-009, MVP-011–MVP-021, MVP-023, MVP-024, MVP-025; all delivery gates through SHARING-SECURITY-READY |
| MVP-023 | Done | FoodController planner core | MVP-002, MVP-009, MVP-012, MVP-014 |
| MVP-024 | Done | Cover My Week experience | MVP-013, MVP-015, MVP-023 |
| MVP-025 | Done | Planner fixtures and invariant tests | MVP-023 |
| PRE-003 | Draft | iOS toolchain readiness (post-launch) | PRE-002, DEC-005 |
| OPT-001 | Draft | Structured URL recipe import | MVP-008 |

MVP-010 was explicitly cut on 2026-08-29 under the PRD's photo deferral rule (owner decision at SEQUENCE step 17): no code, schema, or later card references recipe photos, and MVP-011 packages text-only starter content. It stays in the register as `Cut` and may return as a post-launch card. OPT-001 has no dependency path into MVP completion.

### D-035 migration baseline for existing Done cards

The recursive dependency gate applies prospectively to transitions made under D-035. On 2026-08-26, the six cards already Done were audited once to establish the starting closure:

- `DEC-001`: completion criteria PASS — decisions 2–3 were resolved and decision 1 was explicitly descoped to `DEC-004`. Its `FAIL` clearance observations are the evidence supporting that decision, not failed decision-card completion criteria; its two historical `NOT VERIFIED` observations remain recorded under the pre-D-027 practice.
- `PRE-001`, `MVP-001`, `DEC-002`, and `DEC-005`: their required completion items are recorded as PASS in the evidence log or decision resolution.
- `PRE-002`: AC-1–AC-7 PASS; AC-8 is `NOT VERIFIED` under the fully recorded D-027 exception with `owner-accepted 2026-08-24` and discharge at `PRE-003`.

These six statuses are the grandfathered starting state, not a relaxation for future work. After D-035, a direct dependency counts as successfully Done only when its evidence row satisfies the current PASS/D-027 contract and its owner-approved Done transition is recorded. Because each future Done transition applies that same rule, checking direct dependencies proves the later transitive closure without re-reading every ancestor.

## MVP acceptance traceability

| PRD_v2 §24 item (historical) | Owning cards |
|---|---|
| 1 | MVP-004, MVP-006 |
| 2 | MVP-005, MVP-006, MVP-009 |
| 3 | MVP-007, MVP-008, MVP-011 |
| 4 | MVP-014 |
| 5 | MVP-005, MVP-012, MVP-013 |
| 6 | MVP-005, MVP-006, MVP-013 |
| 7 | MVP-015, MVP-016 |
| 8 | MVP-017 |
| 9 | MVP-020, MVP-021 |
| 10 | MVP-017 and `MVP_INVARIANTS.md` |
| 11 | MVP-019 |

The table above is historical (D-028). The authoritative acceptance mapping is PRD v3 §26, derived 2026-08-26 with the cards (D-034). This table is the single authority for §26 traceability; cards do not repeat it, and `MVP-022` AC-1 reads from it.

| PRD v3 §26 item | Owning cards |
|---|---|
| 1 — Flutter calls the Rust core in Android **and iOS** release pipelines | **Split.** Android half: PRE-002 (Done 2026-08-24). iOS half: PRE-003, post-launch under D-015 and invariant 14 — **not an MVP criterion** |
| 2 — Rust owns authoritative local food/control state in SQLite | MVP-002, MVP-005, MVP-007, MVP-012, MVP-014, MVP-015 |
| 3 — User can start without a mandatory durable account | MVP-004, MVP-006 |
| 4 — Household/member structure exists | MVP-002, MVP-004 |
| 5 — User can quickly create/select meal stubs and recipes | MVP-007, MVP-008, MVP-011, MVP-012 |
| 6 — Restrictions and a small number of food policies are represented | MVP-006, MVP-009, MVP-023 |
| 7 — Manual and locked meals are respected | MVP-012, MVP-013, MVP-023 |
| 8 — **Cover My Week** generates a coherent plan locally, without LLM or network | MVP-023, MVP-024 |
| 9 — Known hard constraints are never knowingly violated | MVP-009, MVP-023, MVP-025 |
| 10 — Planner evaluates the cycle as a sequence | MVP-023, MVP-025 |
| 11 — Important unresolved assumptions surface; low-value questions stay silent | MVP-023, MVP-024 |
| 12 — Shopping list is derived immediately from the accepted plan | MVP-015, MVP-016, MVP-024 |
| 13 — Core loop works offline | MVP-017 |
| 14 — Planner runs are versioned and reproducible enough to debug | MVP-023, MVP-025 |
| 15 — Ordinary behavior creates structured evidence for future cycles | MVP-012, MVP-023, MVP-024 |
| 16 — Analytics measure coverage, corrections and attention without exporting sensitive content | MVP-019 |
| 17 — The product demonstrates the central claim | MVP-024 (attention measurement, AC-8), MVP-022 (go/no-go verdict, AC-6) |

`MVP-003` discharges no §26 item directly; it is the UI carrier for items 5, 7, 8 and 12.

## Decision record

| ID | Decision | Consequence |
|---|---|---|
| D-001 | This roadmap is the operational source of truth | Update it at every task handoff |
| D-002 | Use bounded, outcome-oriented cards | Split work when verification cannot stay coherent |
| D-003 | Resolve classified decision gates before dependent work | Do not bury consequential choices in implementation |
| D-004 | Rebuild from a clean-room scaffold | Preserve no legacy implementation |
| D-005 | Establish a thin domain spine before vertical UI work | Avoid both screen-first coupling and premature architecture |
| D-006 | Do not use Sinter or a custom orchestrator | Use explicit human-reviewed workflow invocations |
| D-007 | Nothing in the old Meal Mate app must be preserved | Only `.git`, docs, license, and selected product metadata survive reset |
| D-008 | Introduce Firebase in phases | Emulator first, real non-production project later, production separately |
| D-009 | Split sharing into publication/web and Android routing | Security boundary is independently verifiable |
| D-010 | Keep observability a separate foundation card | Product work owns its own events afterward |
| D-011 | Bound offline promises to the MVP core loop | Do not add a second database without evidence |
| D-012 | Use risk-tiered verification | Independence and evidence rise with failure cost |
| D-013 | Restate only load-bearing PRD constraints in cards | Keep cards usable without copying the PRD |
| D-014 | Use a curated narrow-rights starter-content hybrid | Original + verified federal/public-domain/CC0; attributed CC BY only when durable |
| D-015 | Android-only MVP/initial launch; iOS post-launch priority | No iOS hardware or CI blocks MVP |
| D-016 | URL import is an isolated MVP stretch goal | Core manual recipe flow remains sufficient |
| D-017 | Classify tasks by complexity and assurance independently | Model capability and verification rigor are separate decisions |
| D-018 | Use guarded workflow routing without automatic chaining | Explicit invocation and review remain control points |
| D-019 | Retire `MealMate` as the public brand; defer name and production ID to DEC-004 | `com.mealmate.app` is consumed on Play and the name is crowded. Scaffold with temporary `dev.mealmate.temp` / Dart `meal_mate`; replace before MVP-018, MVP-021, MVP-022 |
| D-020 | Pin Android `minSdk 24` explicitly | `targetSdk`/`compileSdk` inherit Flutter; PRE-001 verifies the default does not exceed 24 |
| D-021 | Scaffold Android only | Web added at MVP-020 only after deciding Flutter Web vs static Hosting for previews |
| D-022 | Run the Android emulator on the Windows host; WSL uses `adb` as a TCP client | No `/dev/kvm` on this Win10 Pro 22H2 host and nested virtualization is Win11-only. Applies to PRE-001 and the MVP-003/MVP-004/MVP-005 emulator evidence |
| D-023 | Riverpod (non-codegen) + go_router; plain-Dart domain/repository interfaces with Riverpod-override DI | Provider overrides are the test seam; go_router's URI routing serves MVP-020/021 App Links. See DEC-002 resolution |
| D-024 | Feature-first layout (`lib/domain`, `lib/data`, `lib/features/<name>`); no codegen | Maps to PRD §9 entities and MVP card boundaries; avoids build_runner for MVP-sized models. See DEC-002 resolution |
| D-025 | Tiered 100% coverage gate on `lib/domain`/`lib/data` only, plus `glados` property-based tests on the highest-risk domain functions | Blanket 100% on presentation code is low-signal; property tests catch what line coverage can't. `mutation_test` deferred until domain logic is substantial. See DEC-002 resolution |
| D-026 | Domain/Flutter-Firebase independence enforced by a small grep-based script in the command contract | Checks the one named layering rule at negligible cost; upgrade to `clean_code_lints`/`import_lint` only if a second rule emerges. See DEC-002 resolution |
| D-027 | Allow Done with a `NOT VERIFIED` item when the resolving action is not the card's own work to do and the owner accepts the residual risk | Three conjuncts, all required: the card names the constraint or stop condition putting the action outside its scope; the card names the downstream discharge point or the permanent residual risk; the owner records dated, attributed acceptance in the Evidence log. An item that is merely unfinished work inside the card's own scope still blocks Done. Applies to cards reaching Done on or after 2026-08-24. Introduced for DEC-004's Play package-ID item; DEC-004's trademark item is outstanding owner work and is not covered. See `docs/task/README.md` verification rules |
| D-028 | Adopt PRD v3: Rust household kernel + food domain + SQLite behind a coarse FRB bridge; Flutter owns presentation | D-024–D-026 stand for Dart presentation code but no longer govern durable domain state; `PRD_v2` is historical context; MVP-002 retitled to the Rust kernel/SQLite foundation; PRE-002/DEC-005 gate all domain work. Recorded 2026-08-24. DEC-002 moves from the DOMAIN-READY gate to EMULATOR-PERSISTENCE-READY (it governs Dart presentation) and reaches Done on already-recorded evidence: its only open item was the superseded `lib/domain` coverage gate |
| D-029 | Reconcile the task system by dated amendment banners plus outline cards, not a full rewrite | Full re-derivation of affected cards happens once per card after DEC-005 is Done; banners never delete `PRD_v2` citations; a dependency change edits the card's Depends-on cell, the banner, and this register together |
| D-030 | Commit to the Rust core on the FRB native-assets backend; `flutter test` requires the pinned Rust toolchain (`cargo build --release` in `rust/` first); bridge crate stays `rust/` and becomes the workspace root at MVP-002; `rusqlite` (`bundled`) + `rusqlite_migration`; iOS re-opens the commitment at PRE-003 | DEC-005 resolution on PRE-002 evidence (2026-08-24, Android only — declared deviation from PRD §17/§22). Reversal: one `integrate --integration-backend cargokit` run, APIs unchanged; §22 fallback rejected as contra-evidence |
| D-031 | The Status contract above carries the only Draft-trigger rule; `docs/task/README.md` points at it, and a package added inside a card's stated scope is card evidence, not a Draft event | `docs/ROADMAP.md` and `docs/task/README.md` previously enumerated different, non-overlapping triggers ("source or decision" vs "product, architecture, or dependency") with no stated precedence, so every new package was a judgment call. The merged sentence is a superset of both; "dependency" resolves to the card's `Depends on` cell, which is what D-029 already meant by a dependency change. Recorded 2026-08-25 after MVP-002 added `path_provider` and `tempfile`; that disposition (evidence record, no return to `Draft`) is what this rule now states generally. A package that moves an architectural boundary — a backend SDK, an auth provider, a second state-management library — is an architecture change and still returns the card to Draft |
| D-032 | The owner-work hash guard covers the decision cards, the PRD, and the v3 docs; `KNOWN_ISSUES.md` and `KNOWN_ISSUES-low.md` are excluded as review-workflow artifacts | `/redteam-code`, `/fix-findings`, `/ki-maintain`, `/tidy`, `/next-task` and `/import` all write the `KNOWN_ISSUES` files by design, so any card followed by a review pass broke the guard and made a card's own recorded evidence false — MVP-002's did. Hashing cannot distinguish a workflow append from an agent rewriting owner prose; `git diff` at commit time surfaces the latter. Recorded 2026-08-25. PRE-002 AC-6 stays honestly passed; future AC-6-style criteria are checkable again. Rejected: an append-only `git diff` assertion replacing `sha256sum -c` — the repo has no enforcement artifacts beyond the `Zone.Identifier` pre-commit hook, and `KNOWN_ISSUES-low.md` already tracks enforcement routes named but never built |
| D-033 | Emulator bring-up and adb access go through `tools/emulator.sh` (`up`/`adb`/`status`/`down`); agents use `tools/emulator.sh adb <args>` because shell env does not persist across tool calls | Codifies the D-022 bridge shape with the measured host quirks (process names, snapshot revert, socket derivation). Recorded 2026-08-25 |
| D-034 | The v3 amendment banners are discharged in one pass rather than one card at a time. Re-derivation replaces a superseded `PRD_v2` assumption only with a v3 fact already decided in `docs/PRD_v3.md`, `docs/HOUSEHOLD_CONTROL_PRINCIPLES.md`, `docs/CODEX_CLAUDE_PIVOT_PROMPT.md`, `docs/V3_MIGRATION_PLAN.md`, `docs/task/MVP_INVARIANTS.md`, or a recorded D-number; where none of those decides the question the card names a decision gate instead of choosing | D-029 permitted either shape and `docs/task/SEQUENCE.txt` step 1 chose the single pass. Recorded 2026-08-26: 21 cards re-derived (`MVP-004`–`MVP-022`, `DEC-003`, `OPT-001`) and `MVP-023`, `MVP-024`, `MVP-025`, `PRE-003` given bodies. Cards are tiered by who decides their content, not by edit depth: for `MVP-004`–`MVP-017` and `OPT-001` PRD v3 decides, so they are fully re-derived; for `MVP-018`–`MVP-022` and `DEC-003` the cloud, security, environment-topology, App-Check, sharing-rights and release-control content is preserved verbatim because `DEC-003` and `DEC-004` have not decided it yet, and rewriting it here would be the premature specification principles §25 forbids. Three AC-level edits are still made in that second group where v3 itself decides the answer — `MVP-018`'s new AC-6, `MVP-019`'s PRD §19 metric list, `MVP-022`'s AC-1. `MVP-003` is excluded: a current approved plan must re-derive the card before implementation; if no accessible current plan is supplied, `docs/task/SEQUENCE.txt` requires creating and approving one first. `MVP-003`'s banner was the last outstanding remainder; step 2 of its approved plan folded it in on 2026-08-28, completing this pass. The later workflow/status reconciliation changed only operational metadata, not that card scope. D-033 is reserved for `tools/emulator.sh`, so the gap between D-032 and this row is expected and is not a lost decision. `docs/task/SEQUENCE.txt` steps 1–2 are discharged by this row. The sequence and compact current/next fields were reconciled on 2026-08-26 when the mandatory workflow gate was centralized, without changing the intended MVP-003 implementation scope. `DEC-003` is a D-032-guarded decision card edited by design here: only its Authoritative sources and Locked constraints change, and its Decisions-to-resolve and Options-and-tradeoffs sections are byte-unchanged |
| D-035 | Keep `docs/ROADMAP.md` as the sole live task-status source; task-card status cells point to its register, and every plan and execution repeats one shared planning/execution/handoff contract | A second `MVP_STATUS.md`/`TASK_STATUS.md` would split status from the roadmap's evidence and decisions, while a Markdown parser would add a new process failure mode. The compact Current milestone block names the single implementation lane and selected next task. The one-time migration audit above establishes the six pre-D-035 Done cards; later direct dependencies are sufficient because every new Done transition recursively requires valid evidence and explicit owner approval. Execution rechecks current state and plan freshness, then status, evidence, delivery-gate progress, and next task change together at handoff. Owner-paced decisions may proceed concurrently without occupying the implementation lane. Recorded 2026-08-26 after owner selection of the lowest-friction option; corrected by the same-session adversarial review before use |
| D-036 | Fix pass on the D-034 re-derivation: invariant 7 re-derived to the v3 local-first model; §26 item 17 split into `MVP-024` AC-8 measurement and `MVP-022` AC-6 verdict, with the pass bar left to an owner decision; the §26 table made the sole §26 citation site | Invariant 7 still mandated the cache/prefetch model `MVP-017` was re-derived to remove, and `docs/task/MVP_INVARIANTS.md:3` makes a conflict a stop condition; the new wording is anchored to PRD v3 §6.4–6.5 and §12 and the header notes the restatement. `MVP-001` and `DEC-001` cite the invariants file wholesale and are Done, but a scaffold card and a naming decision are immaterial to offline persistence, so neither returns to Draft under D-031. Item 17 previously resolved to `MVP-022`, whose AC-1 discharged it by reading the table that named `MVP-022`; production now sits with PRD §22's named proof vehicle and judgement with the go/no-go, neither repeating the table. This row extends D-034's "Three AC-level edits" enumeration to five: `MVP-019`'s AC-5 and `MVP-022`'s AC-6 are added because PRD v3 §19 decides the metric set and §22 decides that Cover My Week is the required proof — but neither decides a numeric pass bar, so that is recorded as a `MVP-022` decision gate rather than chosen here. `MVP-024` repeated the §26 item→card mapping in its sources and scope, at two sites, one claiming items the table assigns elsewhere; both were removed and its AC-4 extended to veto so the item-15 assignment is fully discharged. `MVP-022` AC-1 still restates table row 1's iOS split and hard-codes the "items 2–17" range — the same defect class surviving at the release gate, left in place because the D-034 tier keeps `MVP-018`–`MVP-022` content verbatim pending `DEC-003`/`DEC-004`, and tracked in `KNOWN_ISSUES.md`. `MVP-006` gained a criterion for member-scoped preference storage and a decision gate for restriction scope, which PRD v3 leaves open. All cards whose content changed were already `Draft`, so no D-031 transition was required. Recorded 2026-08-26 after two adversarial plan reviews |

## Evidence log

| Date | Card | Result | Evidence |
|---|---|---|---|
| 2026-08-21 | DEC-001 | Done | Resolution recorded in the card; decisions 2–3 final, decision 1 descoped to DEC-004. Clearance for `MealMate`: Play package ID — FAIL (`com.mealmate.app` search-index entry; listing URL now 404, i.e. indexed then unpublished, and Play never releases a published ID). Play title — FAIL (≥5 apps titled MealMate). Web/search — FAIL (mealmateco.com, same concept). Trademark — NOT VERIFIED (web-sourced USPTO 97352318, appliances class; no authoritative TESS search). Apple App Store title — NOT VERIFIED (not performed). Recorded under the pre-2026-08-24 practice; predates D-027, whose exception does not apply retroactively. |
| 2026-08-22 | PRE-001 | Done | AC-1 PASS (Flutter 3.47.1 / Dart 3.13.1 at `~/development/flutter`; WSL JDK 17.0.19 with JRE-8 retained as system default; Windows JDK 17.0.20.1; Gradle 9.3.1; Android platform-36, build-tools 36.1.0, platform-tools 37.0.1, cmdline-tools 15859902, emulator 37.1.11, `system-images;android-36;google_apis;x86_64` rev 7; template defaults minSdk 24 / targetSdk 36 / compileSdk 36 — minSdk matches the D-020 floor exactly, no flag needed). AC-2 PASS (`dart format` clean, `flutter analyze` no issues, 1/1 test). AC-3 PASS (`app-debug.apk`, 150MB). AC-4 PASS (Pixel 5 AVD `pre001_avd`, Android 16/API 36, WHPX usable per `-accel-check`; app installed, launched and screenshotted from WSL over the D-022 adb TCP bridge — evidence `C:\pre001_evidence\pre001_evidence.png` + emulator logs, WSL copy `~/pre001_evidence/`). AC-5 PASS (before/after `git status` identical; only pre-existing untracked `.claude/`; no Meal Mate application file changed). See PRE-001 command contract below. |
| 2026-08-24 | DEC-002 | Done | Decision fields present — PASS (rationale/versions/rejected-alternatives/test-implications/reversal-cost recorded for all four sub-decisions: (1) Riverpod non-codegen + go_router over plain-Dart domain/repository interfaces; (2) feature-first layout, no codegen; (3) tiered 100% coverage gate on `lib/domain`+`lib/data` with `glados` property-based tests on the highest-risk domain functions; (4) grep-based script enforcing domain independence from Flutter/Firebase). Commands runnable in the project environment — PASS (`dart format`, `flutter analyze`, `flutter test` exercised at PRE-001 AC-2 and MVP-001 AC-3; `flutter build apk --debug` at PRE-001 AC-3 and MVP-001 AC-3. The `lib/domain` 100% gate and its `lcov`/`genhtml` tooling are superseded by D-028 and were never installed; the `--coverage` flag had no other consumer and is dropped from the contract — `lib/features` coverage reporting is optional and non-gating until a card asks for it. Flipped to Done 2026-08-24 under D-028). Contract recorded in roadmap — PASS (see D-023–D-026 and the DEC-002 command contract below). |
| 2026-08-22 | MVP-001 | Done | AC-1 PASS (`git diff --stat HEAD` = 124 files, 257 insertions, 4124 deletions; `ios/ macos/ windows/ linux/ web/` deleted in full, plus `lib/models/recipe.dart`, `lib/screens/recipe_management/recipe_form_screen.dart`, `test/models/recipe_test.dart`, `android/app/src/main/kotlin/com/example/meal_mate/MainActivity.kt` and all three Groovy `*.gradle` files; independent depth-2 sweep found only the approved surviving set plus stock scaffold output). AC-2 PASS (`applicationId`/`namespace`/`package` = `dev.mealmate.temp`, Dart package `meal_mate`, `android:label="MealMate (dev)"`, explicit `minSdk = 24` with `targetSdk`/`compileSdk` still inheriting `flutter.*` per D-020; zero `com.example` on the code surface — the only two repo-wide occurrences are governance prose in this card and DEC-001; temporary-pending-DEC-004 documented in `README.md`, `pubspec.yaml`, and `build.gradle.kts`). AC-3 PASS (`dart format` clean, `flutter analyze` no issues, `flutter test` 1/1 — all reran independently by the verifier; `flutter build apk --debug` → `app-debug.apk` 150,419,440 bytes. The APK path was permission-blocked in both agent sessions and was confirmed directly by the owner on 2026-08-22). AC-4 PASS (fresh-context verifier: no legacy Dart/schema/test, no non-Android platform tree, `.metadata` `migration.platforms` = root + android only, no residual Groovy, no empty orphan package dir; every scaffold config byte-diffs to stock Flutter 3.47.1 apart from the three intended identity edits). Surviving set = 9 entries (`.git .claude .idea .githooks .gitattributes docs LICENSE KNOWN_ISSUES.md README.md`), owner-approved 2026-08-22 — this deliberately exceeds the card's literal four-entry list. Rollback `~/mvp001_rollback_2026-08-22/` (tarball rehearsed byte-faithful before the destructive step). Not committed or pushed. |
| 2026-08-24 | DEC-004 | In Progress | Shortlist prescreen only; no name chosen, so no clearance item is cleared for a final name. Google Play — NOT VERIFIED (title triage across 12 candidates by two independent methods; lead `Kimatta` shows no titled match, but Play's own search under-reported at least one real listing — `com.orangefox22.Kondate`, a meal-planning app — so a null is "not found by two methods", not "absent"; package-ID availability for `app.<name>` is unprovable read-only, so every package-ID cell for a screened name reads `NOT VERIFIED`. The listing-URL 404 plus no-index-hit observation is recorded for the lead `app.kimatta` alone — see the card's **Kimatta** evidence block; no per-name package-ID observation is recorded for the other ten screened names. Play Console was not used). Apple App Store — NOT VERIFIED (iTunes Search API, the 11 screened names; `Kimatta` no match; exact out-of-category titles found for Shitaku/Provi/Norra; in-category titles found for Kondate). Web/search — NOT VERIFIED (`Kimatta` first page is dictionary entries only; `kimatta.app` is unregistered per RDAP, `kimatta.com` registered 2004-06-01; no domain purchased). Trademark — NOT VERIFIED (`Kimatta` only; tmsearch.uspto.gov returned HTTP 200 / 125,660 bytes of SPA shell with zero query-term occurrences, API paths 405 and 404 — receipt in the card; secondary web search found no record, indicative only; authoritative search needs an interactive session or a paid search and is routed to the owner). Rejected at Pass 1: Kondate, Savora (both in-category Play title collisions). Register, dispositions and per-name evidence in the card. This row is edited in place when the card resolves — flip Result to `Done` and each criterion to `PASS`, `FAIL`, or `NOT VERIFIED`; do not append a second DEC-004 row. Only the package-ID criterion is eligible to stay at `NOT VERIFIED` at Done, and only with the owner's dated, attributed acceptance of the residual risk recorded in this same cell as `owner-accepted YYYY-MM-DD`, per D-027 and `docs/task/README.md`. The trademark criterion is outstanding owner work, not an exception case, and blocks Done until performed. |
| 2026-08-24 | PRE-002 | Done | AC-1 PASS (`rustc 1.98.0`, `cargo 1.98.0`, `flutter_rust_bridge_codegen 2.13.0`; pins in `rust/rust-toolchain.toml`, `pubspec.yaml`, `rust/Cargo.toml`; backend native-assets — see the PRE-002 command contract). AC-2 PASS via the native-loading test route: `test/bridge_native_test.dart` 2/2 under `flutter test`, `throwsA(isA<KimattaError_InvalidPath>())` on a whitespace path; coupling recorded in the contract for DEC-005 decision 3. AC-3 PASS (`app-debug.apk`). AC-4 PASS (`app-release.apk` 46.0 MB; `libkimatta_bridge.so` in `arm64-v8a` 676,480 B, `armeabi-v7a` 429,212 B, `x86_64` 704,128 B; debug carries the same set). AC-5 PASS (release APK on `emulator-5554` over the D-022 bridge; `~/pre002_evidence/health_screen.png` shows `core_version: 0.1.0` and `health: schema v0 at …/kimatta.db`, `typed_error.png` shows `probe: KimattaError.InvalidPath` after tapping the button located by its semantics `content-desc`). AC-6 PASS (`sha256sum -c` 38/38 OK over every staged owner path; index blobs of the four docs this card edits were byte-identical before staging). AC-7 PASS (11.05 / 10.32 / 9.33 s). AC-8 NOT VERIFIED — D-027 block: no macOS host or CI exists and iOS is post-launch (D-015, invariant 14); discharge point `PRE-003`; owner-accepted 2026-08-24 (recorded by the agent at the owner's explicit instruction). Fix ledger: 1 of 2 native-assets fixes used, cargokit not attempted. Card promoted to Done on that clause. Not committed or pushed. |
| 2026-08-24 | DEC-005 | Done | Decisions 1–5 recorded in the card with rationale, rejected alternatives, test implications, and reversal cost — PASS. Registered as D-030 — PASS. Android-only scope of decision 2 stated in D-030 and in `docs/V3_IMPLEMENTATION_STATUS.md` Deviations — PASS. MVP-002 promoted Draft → Ready. Not committed or pushed. |
| 2026-08-24 | MVP-002 | Done | AC-1 PASS (`cargo test --workspace`: household-core 2/2 — empty/whitespace rejected, no trimming; kimatta-storage 7/7 — empty DB → v1, `MIGRATIONS.validate()`, foreign keys on after `open()`, orphan member rejected via FK, rollback on duplicate later row, round trip, idempotent file reopen keeps version 1 and data; `cargo tree --workspace -i rusqlite` = one version). Resolved: `rusqlite` 0.40.2 (`bundled`), `rusqlite_migration` 2.6.0, `libsqlite3-sys` 0.38.2. AC-2 PASS (`cargo fmt --all --check`, `cargo clippy --workspace --all-targets -- -D warnings` clean; `grep -rn unsafe rust/crates/household-core rust/crates/kimatta-storage` empty; both crates `#![forbid(unsafe_code)]`). AC-3 PASS (verified on-device 2026-08-25 over the D-022 bridge, `emulator-5554`, AVD `pre001_avd`, API 36 x86_64: release APK installed and launched, health screen reads `health: schema v1 at /data/user/0/dev.mealmate.temp/files/kimatta.db`, probe reads `probe: KimattaError.InvalidPath`, no `AndroidRuntime` exception; `ac3_health.png` / `ac3_probe.png` in `~/mvp002_evidence/`). **The D-027 block is discharged — no `owner-accepted` clause is needed.** The database path is `getApplicationSupportDirectory()` → `context.getFilesDir()` rather than `Directory.systemTemp`; verified upstream that `Directory::SystemTemp()` falls back to the literal `/data/local/tmp` on Android when `TMPDIR`/`TMP` are unset and that the Flutter Android embedding sets neither, so the pre-fix build would have failed this AC outright with `KimattaError.Storage`. AC-4 PASS (Rust gate added to the DEC-002 contract and mirrored in the PRE-002 block and README, and ordered ahead of the Flutter lines to match PRE-002 so a Rust failure precedes the APK build; exercised clean, plus `flutter test` 5/5 — the bridge tests now assert schema version 1 on a real file and `KimattaError_Storage` on an unopenable path). Cross-compile: `rusqlite bundled` built under the native-assets hook first try (debug APK 70.7 s cold). All four required items are PASS and no D-027 exception is needed; owner-approved 2026-08-27 in the instruction to handle closeout. `Verify → Done`; DOMAIN-READY achieved; `MVP-003` promoted `Draft → Ready`. Not committed or pushed. |
| 2026-08-28 | MVP-003 | Verify | AC-1 PASS (`test/app_test.dart` 14/14 within `flutter test` 18/18: five destinations, `/plan/cover` reachable and returnable, unknown route renders not-found with a working return action, restoration with a fresh router — the restoration test is an expected-to-pass regression pin with no falsifiability control, because `MaterialApp.router` restores the location through Flutter's own `Router` under the `'app'` scope rather than through go_router's ids; it pins this card's composition, not those ids). AC-2 PASS (three guidelines — `androidTapTargetGuideline`, `labeledTapTargetGuideline`, `textContrastGuideline` — × light/dark × five destinations; destination screens at text scale 2.0, `NavigationBar` labels clamped at 1.3 by framework design; focus automated via tab traversal). All AC-2 items come from `flutter test` and are bridge-independent. AC-3 PASS (gate exit codes all 0: `dart format`, `flutter analyze`, `cargo fmt`/`clippy`/`test --workspace` 9/9, `cargo build --release` + `flutter test` 18/18, `flutter build apk --debug`. On-device 2026-08-28 over the D-022 bridge, AVD `pre001_avd`, API 36 x86_64, 1080×2340 gesture navigation; evidence `~/mvp003_evidence_wt4/`: `plan.png`, `settings.png`, `db_listing.txt`, `settings_scale2.png`, `relaunch_after_kill.png`; pids before=3437 relaunched=3904. Screenshot readings: `plan.png` shows the Plan destination selected; `settings.png` shows `Local database: schema v1 at /data/user/0/dev.mealmate.temp/files/kimatta.db`; `settings_scale2.png` shows visibly scaled body text with no overflow while the bar labels stay clamped; `relaunch_after_kill.png` shows Settings still selected after `am kill`. `run-as dev.mealmate.temp ls -l files/kimatta.db` → `-rw------- 1 u0_a218 u0_a218 20480 2026-08-25 09:33 files/kimatta.db`, a real app-private file entry — but one that predates this run: the 2026-08-25 mtime is MVP-002's on-device session, so what this run exercised is reopen of an already-v1 database, not create-and-migrate. The migration is idempotent and left the schema at v1. No on-device first create has been observed on any card; tracked as an OPEN entry in `KNOWN_ISSUES.md`). Packages: go_router 18.0.0, flutter_riverpod 2.6.1 (D-031); the resolved Riverpod major is 2.x, which has no provider-retry behaviour to disable. Measured: `StatefulShellRoute.indexedStack` builds branches **lazily** on go_router 18.0.0 — established by the "the health provider is created once per launch" control, which read `creations == 0` with `App`'s `ref.listen` removed. Evidence contract satisfied; awaiting explicit owner approval. Not committed or pushed. |
| 2026-08-28 | MVP-003 | Done | Owner approval recorded on the adversarial review at `~/.claude/reviews/redteam-mvp003-integration-verify-2026-08-28T1904-03fd.md`: gates reproduced independently on `integration` (`cargo build --release` 0, `flutter analyze` 0, `flutter test` 18/18); DB path `getApplicationSupportDirectory()`, single startup bridge call, `/plan/cover` nested under Plan, honest placeholders, no illegitimate status writes — all confirmed; six prior findings verified landed; remaining findings are LOW/record-accuracy and tracked in `KNOWN_ISSUES-low.md`. The two MEDIUM record findings (dead `.orch` review links; calibration paragraph counts) are corrected in this commit. `Verify → Done`; `MVP-004` promoted `Draft → Ready` and selected as the next implementation task. Implementation is on `integration` as `844d0f4`; not on `master`. |
| 2026-08-28 | MVP-004 | Verify | AC-1 PASS (`kimatta-storage` `ensure_creates_once_then_reuses` + `ensure_persists_across_reopen`; bridge `bootstrap creates one household once and reuses it`, `reopening the same file keeps the household (restart)`; on-device first run created household `bdfe3373-…` / member `ed853676-…` name NULL, display_name `Me`). AC-2 PASS (`load_household_scopes_members`, `rename_touches_only_the_named_household`, `rename_unknown_household_is_an_error`, `mismatched_member_rejected`; bridge `rename of a foreign id is rejected and changes nothing`, `a different file is a different household`). AC-3 PASS (`tools/emulator.sh reset` = `pm clear` at 04:05:54Z, first launch created `files/kimatta.db` with post-clear mtime `2026-08-28 23:05` device-local — the first on-device first-create observed on any card; `am force-stop` + relaunch: `kimatta_first.db` and `kimatta_relaunch.db` hold identical ids, counts 1/1; `logcat -s AndroidRuntime` shows only uiautomator VMs, no `FATAL EXCEPTION`). AC-4 PASS (both launches in airplane mode — `settings get global airplane_mode_on` = 1 — rendered Settings and Household; `android/app/src/main/AndroidManifest.xml` has no `INTERNET`; pubspec has no network package; `cargo tree` has no `reqwest`/`hyper`/`firebase` — `tokio` is present as a pre-existing `flutter_rust_bridge` transitive, unchanged from MVP-003). AC-5 PASS (widget tests: Settings summary `Unnamed household · Just you for now — a household of one.`, tile → `/settings/household` and back, identity + rename controls, storage failure in prose, three a11y guidelines × light/dark; on-device uiautomator dumps read the same strings, `content-desc="Save name"`, hint `Household name`; TalkBack not scriptable — guideline coverage plus screenshots `settings_first.png`, `household_first.png`, `household_relaunch.png` under `~/mvp004_evidence/`). Gates: `dart format`, `flutter analyze`, `cargo fmt`/`clippy`/`test --workspace` 17/17, `cargo build --release` + `flutter test` 30/30, `flutter build apk --debug` all rc 0; `cargo tree -i rusqlite` one version. **Superseded by the redteam fix pass (`.orch/redteam-impl-handoff-orch-6-2026-08-28T2327-6b40.md`), which added tests: `cargo test --workspace` is now 22/22 and `flutter test` 38/38, both rc 0, as are `cargo fmt`/`clippy`/`dart format`/`flutter analyze`. The 17/17 and 30/30 figures describe the pre-fix tree and are kept so the original run stays legible.** Package added: `uuid` 1.26.0 (`v4`; D-031, in-scope). `KNOWN_ISSUES.md` entries for `api/error.rs`, the re-export, connection ownership, the mis-parent guard, and the first-create observation are RESOLVED; `KNOWN_ISSUES-low.md` production-path carrier RESOLVED. Not committed or pushed. |
| 2026-08-29 | MVP-005 | Verify | AC-1 PASS (`kimatta-storage` `planning_cycle_round_trips`, `ensure_creates_the_default_cycle_once_then_reuses_it`, `ensure_cycle_persists_across_reopen`, `saving_replaces_the_whole_slot_set`, `slots_load_in_canonical_order_not_insertion_order`, `planning_reads_and_writes_are_household_scoped`; bridge `save_then_ensure_returns_the_saved_cycle`; Dart `a saved cycle survives reopening the same file`). AC-2 PASS (16 `food-domain` date tests written from the criteria: month, year, February common/leap/century-non-leap, both hemispheres' DST transitions, ISO-vs-locale, malformed parse, datetime/instant rejection, calendar overflow; plus Dart `todayCivilDate uses the local date late at night, not the UTC date` and the bridge round-trip asserting `dates.last == '2026-09-04'`). AC-3 PASS (`dinner_only_is_the_default_scope`, `default_cycle_is_seven_dinner_only_days_from_the_anchor`, `ensure_returns_the_dinner_only_default_with_its_dates`, widget test `Settings shows the dinner-only default cycle without any setup`, and the on-device Settings tile reading `Dinner only · 7 days from 2026-08-29`). Gates all rc 0: `dart format`, `flutter analyze`, `cargo fmt`/`clippy`/`test --workspace` **64/64** (was 22/22), `cargo build --release` + `flutter test` **51/51** (was 38/38), `flutter build apk --debug`, `flutter build apk --release`, `cargo tree --workspace -i rusqlite` one version. **Superseded by the redteam fix pass (`.orch/redteam-impl-handoff-orch-8-2026-08-29T0120-ca0e.md`), which added three tests and changed two production lines: `cargo test --workspace` is now 66/66 and `flutter test` 52/52, both rc 0, as are `dart format`, `flutter analyze`, `cargo fmt`/`clippy`, `cargo build --release` and `flutter build apk --debug`. The 64/64 and 51/51 figures describe the pre-fix tree and are kept so the original run stays legible.** **Decision gate closed:** `jiff` 0.2.35 with `default-features = false, features = ["std"]` (option A1) — `cargo tree` shows no `jiff-tzdb*`; package added inside the card's stated scope, so card evidence, not a Draft event (D-031). Honest limit: dropping the tz *database* does not remove `Timestamp`/`TimeZone`, so the no-instants rule is enforced by `grep -rn "Zoned\|Timestamp\|TimeZone\|::now()" rust/crates/food-domain/src rust/src/api/planning.rs` (empty), not by the feature set. Characterised empirically before the wrapper was written: jiff 0.2.35 *accepts* `20260829`, `2026-08-29T00:00:00-05:00` and `2026-08-29 00:00:00` as civil dates, so `parse_civil_date`'s own `YYYY-MM-DD` shape check is load-bearing; it *rejects* all four shape-identical out-of-range dates, so no extra range check was needed. Cycle length is a stored column, bounds `1..=31`, default 7 — 31 is the largest cycle expressible inside one calendar month, caps the shipped `dates` vector at 31 strings and `MVP-012`'s later occurrences at 93; at the default length ~20% of anchors cross a month boundary, so that case is mainline and has a literal-value test. Both stop conditions checked: no hard-coded week (`7` appears only as `DEFAULT_CYCLE_DAYS`, the ISO dash-position check and test literals) and no UTC conversion (both greps clean; `grep -rn "toUtc\|toIso8601String" lib/features/planning` matches only the comment saying why not). Invariants: 4 — anchor plus stored `length_days`, never a weekday assumption; 17 — `grep -rn "sqlite\|sqflite" lib/` empty, only Rust writes the two new tables; 20 — `PlanningCycle::dates()` is a pure function of stored inputs and reads no clock, so it is deterministic for the same snapshot; reason codes, algorithm version and historical-record immutability have no call site until `MVP-023`/`MVP-025` and are named as such rather than skipped; 21 — two coarse whole-aggregate bridge calls, no SQLite handle crosses, `rust/src/frb_generated.rs` and `lib/src/rust/**` changed only by `flutter_rust_bridge_codegen generate`. On-device (2026-08-29, `emulator-5554`, AVD `pre001_avd`, debug APK installed with `-r` and **no** `pm clear`): the pre-install `files/kimatta.db` was 20480 B / mtime 2026-08-28 23:57; after launch it is 36864 B / mtime 2026-08-29 01:05 with `PRAGMA user_version` = 2, one household row, `planning_cycle` = `2026-08-29` / 7 and one `dinner` slot; `uiautomator` reads `Local database: schema v2 at /data/user/0/dev.mealmate.temp/files/kimatta.db` and `Planning cycle · Dinner only · 7 days from 2026-08-29`; `logcat -s AndroidRuntime` has no `FATAL EXCEPTION`. **Limit, stated honestly:** the household id is `dcb9081f-ff4e-43df-aad1-08a98808b7f1`, not `MVP-004`'s `bdfe3373-…` — the device database had already been recreated by a later session before this card ran, so this run evidences an in-place v1→v2 migration of the database that was there (file grown, not reset), but does not carry MVP-004's original row forward. `an_existing_v1_database_migrates_to_v2_without_losing_data` covers the same migration at test level from a real v1 file via `MIGRATIONS.to_version(.., 1)`. Not committed or pushed. |
| 2026-08-29 | MVP-004 | Done | Orchestrated approval under `# orchestrate: auto-approve`: review `redteam-impl-handoff-orch-6-2026-08-28T2327-6b40.md`; 1 fix round(s); independent verifier `NOT_VERIFIED` (session `c7ad3f7d-6d3c-42e4-a0c0-aad248ebf36c`); merged into `integration` as `af76a4b0f4cd8fbc1321d50ddc4ddb89064800e8`. Owner ratifies at the `integration → master` merge. |
| 2026-08-29 | MVP-007 | Verify | AC-1 PASS (`kimatta-storage` `recipe_round_trips_with_structured_and_original_lines`, `save_replaces_the_whole_line_set`, `recipe_persists_across_reopen`, `list_returns_the_household_summaries_in_title_order`; bridge `save_then_load_returns_an_equal_dto`; Dart `a recipe with structured and original lines round-trips across the bridge`, `a saved recipe survives reopening the same file`). AC-2 PASS (`food-domain` 22 tests incl. `rational_normalises_by_gcd`, `zero_numerator_is_rejected`, `inverted_range_is_rejected`, `rational_ordering_does_not_overflow`, `original_text_is_stored_verbatim`, `blank_title_original_text_name_and_preparation_are_rejected`; storage fixtures `unknown_quantity_and_unit_survive_a_round_trip_as_unknown`, `other_unit_text_survives_verbatim`, `range_and_mixed_fraction_survive_exactly`, `optional_flag_and_preparation_survive`, `a_stub_recipe_with_no_lines_round_trips`, `provenance_round_trips_for_every_kind`, `a_corrupt_quantity_row_is_rejected_on_read`, `an_unknown_unit_text_row_is_rejected_on_read`, `a_recipe_with_no_provenance_row_is_reported_as_corrupt`). AC-3 PASS (`custom_ingredients_are_household_scoped`, `load_of_another_households_recipe_is_none`, `list_never_returns_another_households_recipes`, `a_line_referencing_another_households_custom_ingredient_is_rejected_before_any_write`, `saving_over_another_households_recipe_id_is_rejected`, `recipe_for_an_absent_household_is_rejected`, `a_line_naming_an_absent_ingredient_is_rejected_typed`; bridge `commands_are_scoped_to_the_named_household`, `a_custom_ingredient_of_another_household_is_rejected`; Dart `a recipe id from a different household is not found`, `custom ingredients list only for their household`). AC-4 PASS (fresh-context reviewer against MVP-009/011/012/014/015/020: every card joins by FK with no parallel array; `recipe_provenance` is a sufficient seam — **deferred explicitly**: rights-basis/attribution/modifications/verification-date columns to `MVP-011`, publication columns to `MVP-020`, both as additions on `recipe_provenance` without touching `recipe`). Gates rc 0: `dart format`, `flutter analyze`, `cargo fmt`/`clippy`/`test --workspace` **127/127** (was 66/66: food-domain 43, household-core 2, kimatta-storage 63, bridge 19), `cargo build --release` + `flutter test` **57/57** (was 52/52), `flutter build apk --debug`; `cargo tree -i rusqlite` one version; `flutter build apk --release` was still running when this row was written — see the handoff message. Schema v3 (six tables); migrations tested v1→v3 and v2→v3 (`an_existing_v1_database_migrates_to_v3_without_losing_data`, `an_existing_v2_database_migrates_to_v3_without_losing_data`); FK `NO ACTION` on ingredient refs pinned by `deleting_a_catalog_ingredient_referenced_by_a_line_is_refused`. **Decision gates closed:** A1 exact rationals `numer/denom` (`u32`, gcd-normalised; lcm of recipe denominators ≤16 across a cycle stays under 10⁴, far inside `u32`; `2/4 == 1/2` canonical), `Quantity = Unknown | Exact | Range`; B1 units `tsp tbsp cup fl_oz ml l g kg oz lb piece` + `Other(verbatim)`; C1 `name` always present + `Option<IngredientRef>` over two nullable FKs with the exclusivity CHECK; D1 own `recipe_provenance` table; E1 lines keyed `(recipe_id, position)`, replaced whole. Deferred honestly: open-ended (`2+`) and zero-lower-bound (`0–2`) ranges have no representation — stored as `Unknown` with `original_text` intact. `food-domain` pre-existed from MVP-005; no package added; FRB accepted the data-carrying DTO enums (Risk 1 did not fire). Invariants: 2 — `original_text` column + verbatim tests; 10 — unresolved identity is `NULL`, never inferred; 17 — `grep -rn "sqlite\|sqflite" lib/` empty; 19 — `Quantity::Unknown` explicit, nothing claims safety; 21 — five whole-aggregate bridge calls, generated files changed only by codegen (`git diff --stat`). Not committed or pushed. **Superseded 2026-08-29 by the orch/10 fix pass** (review `redteam-impl-handoff-orch-10-2026-08-29T0230-130e.md`, 6 findings fixed): `cargo test --workspace` now **141/141** (food-domain 46, household-core 2, kimatta-storage 70, bridge 23); `flutter test` **57/57** unchanged (no DTO shape changed, so no FRB regeneration); `cargo fmt --all -- --check` and `cargo clippy --workspace --all-targets -- -D warnings` rc 0. AC-2's corrupt-row evidence gains `a_valid_quantity_kind_with_a_half_present_bound_is_reported_with_its_columns`, `an_unknown_quantity_kind_row_is_reported_as_unknown_kind`, `a_corrupt_unit_kind_row_is_reported_with_its_columns` and `a_known_unit_kind_with_no_unit_text_is_reported_as_corrupt`; AC-3's scoping evidence gains `a_custom_ingredient_id_owned_by_another_household_is_rejected_typed`. Schema v3 additionally indexes both `recipe_ingredient_line` ingredient FKs (`the_line_ingredient_foreign_keys_are_indexed`); `flutter build apk --release` remains the open verifier gate. |
| 2026-08-29 | MVP-005 | Done | Orchestrated approval under `# orchestrate: auto-approve`: review `redteam-impl-handoff-orch-8-2026-08-29T0120-ca0e.md`; 1 fix round(s); independent verifier `PASS` (session `7d8237b0-3ccd-48fb-b040-931f3993c0fa`); merged into `integration` as `a593b8a239994a2be4216f13a3b689056879f1ba`. Owner ratifies at the `integration → master` merge. |
| 2026-08-29 | MVP-007 | Done | Orchestrated approval under `# orchestrate: auto-approve`: review `redteam-impl-handoff-orch-10-2026-08-29T0230-130e.md`; 1 fix round(s); independent verifier `PASS` (session `46d4cb3e-66e6-4fcf-996e-a49790683fbc`); merged into `integration` as `a357c121caf4d516c3de20c40e6ee104583682ea`. Owner ratifies at the `integration → master` merge. |
| 2026-08-29 | MVP-006 | Verify | AC-1 PASS (widget `a first launch lands on Welcome` — `title('Welcome')` found and `find.byType(NavigationBar)` finds nothing, so the first-run screen is outside the shell; `Get started marks onboarding done and reaches Plan` — the `completeOnboarding` hook is called exactly once with `h-1` and Plan renders; `a failed completion still lets the user in` — the hook throws, the `Setup unavailable: disk full` snackbar shows **and** Plan still renders, so a database failure cannot trap the user; `an onboarded household stays on Plan` as the expected-to-pass regression pin for all 57 pre-existing tests. On device: `welcome.png` (first run after `pm clear`, no nav bar), `plan_after_skip.png` (Plan after **Get started**), `plan_relaunch.png` (force-stop + relaunch does **not** return to Welcome — the flag persisted)). AC-2 PASS (storage `restrictions_round_trip_in_stored_order`, `saving_replaces_the_whole_restriction_set`, `saving_an_empty_set_clears_the_restrictions`, `restrictions_are_household_scoped`, `restrictions_persist_across_reopen`; bridge `a_mixed_set_round_trips_in_order`, `saving_returns_the_trimmed_deduplicated_set`; Dart bridge `a saved restriction set survives reopening the same file`, `an empty save clears the restriction set`; widget `the cycle editor saves the edited scope and length` (asserts the exact request: `mealSlots: [lunch, dinner]` in index order after the editor's sort, `lengthDays: 9`, unchanged anchor, `householdId: h-1`), `Start from today sets the anchor to the local civil date`, `Save cycle is disabled with no meal selected`, `the restrictions editor saves checked kinds and free text` (exactly `[Known('peanuts'), Other('nightshades')]`), `a deleted chip stays deleted across the save rebuild`, `a blank free-text restriction cannot be added`, `Settings summarises a set restriction list by label`, plus both Settings tiles navigating and returning. On device: `cycle_saved.png` (Lunch added, `+`×2 → 9 days, saved), `restrictions_saved.png` (Peanuts checked, `nightshades` added, saved), `settings_relaunch.png` — after force-stop + relaunch Settings reads `Planning cycle · Lunch, dinner · 9 days from 2026-08-29`, `Restrictions · Peanuts, nightshades` and `Local database: schema v4 at /data/user/0/dev.mealmate.temp/files/kimatta.db`). AC-3 PASS (widget `Welcome asks for no account` — no `TextField` on the screen and no case-insensitive match for `sign in`/`account`/`password`/`email`; fresh-install walkthrough on `emulator-5554` (AVD `pre001_avd`, API 36 x86_64) after `tools/emulator.sh reset` = `pm clear`, then `install -r` of the debug APK: first launch renders Welcome with two buttons and nothing to fill in). AC-4 PASS (**named evidence** `preferences_are_keyed_by_member_not_household` — two `member_id`s of the *same* household hold different sets and read back distinctly; plus `saving_replaces_the_whole_preference_set`, `preferences_for_a_member_of_another_household_are_rejected`, `preferences_for_an_absent_member_are_rejected`, `a_row_with_an_unknown_sentiment_is_rejected_on_read`, `preferences_persist_across_reopen`, and `deleting_a_household_cascades_to_its_members` extended to assert the member's preference rows go with it). Gates all rc 0: `dart format --output=none --set-exit-if-changed .`, `flutter analyze`, `cargo fmt --all --check`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo test --workspace` **186/186** (was 141: food-domain 59, household-core 2, kimatta-storage 94, bridge 31), `(cd rust && cargo build --release) && flutter test` **90/90** (was 57), `flutter build apk --debug`, `flutter build apk --release`; `cargo tree --workspace -i rusqlite` one version (0.40.2); `grep -rn "sqlite\|sqflite" lib/` empty (invariant 17); `git diff --stat -- rust/src/frb_generated.rs lib/src/rust` shows codegen-only changes plus the two new generated `restrictions` files. Database read-back: `adb exec-out run-as … cat files/kimatta.db` copied 126,976 B, byte-size verified equal to the device's `stat -c %s` before reading; `PRAGMA integrity_check` `ok`, `PRAGMA user_version` `4`, `household.onboarded` `1`, `household_restriction` `0|peanuts|` and `1|other|nightshades`, `planning_cycle` `2026-08-29|9`, `planning_meal_slot` `lunch`/`dinner`. `adb logcat -s AndroidRuntime` has no `FATAL EXCEPTION`. **Inherited gate closed:** `flutter build apk --release` passed at this card's Step 0 baseline and again at Step 13, so `MVP-007`'s open verifier gate (recorded as never run) is discharged; the Step 0.4 exception did not fire. **Schema v3 → v4**, one migration: `ALTER TABLE household ADD COLUMN onboarded` + `household_restriction` + `member_food_preference`, both keyed `(parent_id, position)` and replaced whole, as `recipe_ingredient_line` is. Six version pins updated in the same step — `an_existing_v1_database_migrates_to_v4_…`, `empty_db_migrates_to_v4`, `an_existing_v2_database_migrates_to_v4_…`, `reopen_is_idempotent`, the `bridge_native_test.dart` `schema v4` expectation, and the three `app_test.dart` fake-report sites — with `the_line_ingredient_foreign_keys_are_indexed` **re-expressed rather than bumped** (absent-at-v2 then present-at-v3), because bumping the number would have deleted the claim its comment makes. New migration tests `an_existing_v3_database_migrates_to_v4_without_losing_data` and `a_migrated_household_is_not_onboarded` (no backfill `UPDATE`, so a v3 install reads `onboarded = 0` and is shown the skippable Welcome once after the upgrade). **Decision closed in scope (D-031: no package added, so nothing returns to `Draft`):** the restriction vocabulary is 11 snake_case tokens — `peanuts, tree_nuts, dairy, eggs, gluten, soy, fish, shellfish, sesame, vegetarian, vegan` — plus `Other(verbatim)`; the first nine are the FDA major allergens (milk → `dairy`, wheat → `gluten`), the last two the diet constraints that behave as hard filters. Allergen-vs-diet is deliberately not modelled: `MVP-009` branches on the token when it has a matching rule to attach. Scope is household-wide, per the owner's 2026-08-28 resolution recorded in the card, pinned by `restrictions_are_household_scoped`. **Bridge DTO convention:** `RestrictionDto::Known { kind: String }` carries the token as a string rather than a nested enum, following the reason `rust/src/api/recipe.rs:26-27` already records for `UnitDto` — the vocabulary `MVP-009` may extend must not double the bridge surface. The cost is that Dart's label switch has a `_ => kind` arm instead of compile-time exhaustiveness; it is bought back by the cross-check test `every known restriction kind has a Dart label` in `bridge_native_test.dart`, so a kind added in Rust without a Dart label fails a test rather than rendering `tree_nuts` to a user. **Deliberate deferral:** `member_food_preference` has no writer in the app after this card. That is exactly what the card asks for — AC-4's evidence plan requires only a storage-layer test — and the consumer belongs to `MVP-009`/`MVP-023`; a bridge command and an onboarding question now would be an unconsumed FRB surface and a question with no payoff, which PRD §15 and the card's stop condition both forbid. Read as scoped, not forgotten. **Invariant 10, load-bearing:** the restrictions editor leads with `Warnings only. Meal Mate flags what it knows about; it can never tell you a recipe is safe.` (pinned verbatim by a test), and both editors build their form **only** under `AsyncData` — `_known`/`_other` start empty and Save writes the whole set, so a Save reachable from the loading or error arm would send `[]` and silently clear every stored restriction with nothing on screen to show the loss; `the restrictions editor shows progress and no Save while loading` and `… renders a load failure and no form` pin that shut, asserting on the checkbox list rather than the button so a lazily-built off-screen button cannot make them vacuous. A corrupt row is reported, never coerced: `CorruptRestriction` names household, position, kind and text. **Known limits, stated honestly:** (1) first-frame glimpse — with the listen-and-`go` gate in `App`, a genuine first launch renders Plan for the frames between router build and the household DTO arriving, accepted over a startup splash because a splash would make Settings' own `AsyncLoading` arm unreachable in production and break `test/app_test.dart`'s pending-report test; (2) the cycle anchor is displayed with a **Start from today** button rather than a date picker — no MVP card defines cycle rollover yet, so past/future anchor selection is unbounded UX for no payoff. **Test-harness notes for the reviewer:** `restrictionsProvider` is overridden unconditionally in `harness()` because Settings watches it, so an un-overridden provider would send every Settings-scoped test to the real bridge; the restriction-editor interaction tests use a taller `useTallView` viewport rather than scrolling, because `ListView` builds lazily and a scrolled-to control lands under the app bar where the tap hits the wrong widget — the accessibility and text-scale tests keep Pixel-5 geometry; and the three text-scale checks are separate tests rather than a loop, because re-pumping `harness` with a different `initialLocation` trips `App.didUpdateWidget` and that assertion poisons every later test in the file. **Superseded 2026-08-29 by the orch/12 fix pass** (review `.orch/redteam-impl-handoff-orch-12-2026-08-29T0439-99c7.md`, 5 findings fixed, 3 deferred): five widget tests added, so `flutter test` is now **95/95** (was 90/90) after `cargo build --release`; `cargo test --workspace` stays **186/186** — the only Rust edit is a test doc comment. `dart format`, `flutter analyze`, `cargo fmt`, `cargo clippy` all rc 0. Behaviour changes: Welcome's two buttons wait for `householdProvider` to resolve and the unresolved-id branch now reports `Setup unavailable: the household did not load` instead of navigating on in silence; the restrictions editor's checkbox list, `Add` and chip delete are disabled while a save is in flight; free text naming a known kind ticks that checkbox rather than storing a duplicate `Other`. The migration sentence above was corrected in the same pass — the v4 migration carries no backfill, which is what `a_migrated_household_is_not_onboarded` asserts. Not committed or pushed. |
| 2026-08-29 | MVP-008 | Verify | AC-1 PASS (widget `a new recipe with two ingredient rows is saved as entered` — exact DTO field by field and deep line list: title `'  Pancakes '` and `original_text` sent verbatim with no `.trim()`, `1/2` → exact 1/2, `2-3` → range, known `cup`, `Other('handful')`, optional, `ingredient: null`; `editing seeds the form from the loaded recipe and saves with the same id`; `the recipe library lists summaries and opens the detail`; `the recipe detail shows lines verbatim and the instructions`; bridge-native `a saved recipe survives reopening the same file` (MVP-007) and `an archived recipe survives reopening the same file`. On device (`emulator-5554`, AVD `pre001_avd`, API 36, debug APK `install -r`): `Recipes` tab → `New recipe` FAB → form (`Title`, `As written`, `Name`, `Amount` read from uiautomator `hint`) → `Save recipe` → list shows `Toast`; `am force-stop` + relaunch → list still shows `Toast`; `run-as … cat files/kimatta.db` (126,976 B): `PRAGMA user_version` **5**, `integrity_check` ok, `recipe` rows present with `archived_at` NULL after the restore below. Evidence `~/mvp008_evidence/` (`recipes.png`, `list_after_save.png`, `list_relaunch.png`, `detail.png`, `dialog.png`, `archived.png`, `list_after_restore.png`, `*.xml` dumps, `kimatta.db`). Honest limit: the adb `input text` script typed the line fields out of order, so the on-device row stores `original_text = '2'` — a harness artefact; verbatim text is pinned by the widget and bridge tests). AC-2 PASS (Dart `recipe_fields_test.dart`: blank → Unknown; `2`, `1/2`, `1 1/2`, `1.5`→15/10 exact; `2-3`/`2 - 3`/`2–3`/`1/2-3/4` range; `0`, `1/0`, `3-2`, `abc`, `2+`, `1-2-3`, `0-2`, `-1` → null; widget `a blank title blocks save with an inline error`, `an unreadable quantity blocks save and names the row` (`Ingredient 1: cannot read "abc"`, save hook never called), `a failed save reports the reason and re-enables Save`, `the form shows progress and no Save while the unit vocabulary loads`, `the edit form renders a load failure and no form`; bridge `archive_rejects_a_non_civil_date_as_planning_error` (`2026-08-29T23:00:00Z`, `20260829`, `""` → typed `Planning`, nothing written), Dart `a non-civil archive date is a typed Planning error`, `a decimal quantity is stored as its lowest-terms rational` (15/10 → 3/2); `describeFailure` Recipe arm pinned by `Settings reports a recipe-domain failure in prose`; a blank untouched row is skipped, not rejected; post-review bounds: `the u32 range the bridge carries is the accepted range`, `a mixed number is bounded by its product, not by its groups`, `amounts outside 64 bits are null, never a throw or a wrap`, widget `an over-long amount blocks save and names the row`, `a servings count above the bridge range blocks save`). AC-3 PASS (**policy: archive, never hard-delete** — storage `archive_hides_a_recipe_from_the_active_list_and_lists_it_as_archived` (row count unchanged), `archive_is_idempotent_and_keeps_the_first_date`, `restore_returns_a_recipe_to_the_active_list`, `archive_and_restore_are_household_scoped` (foreign → `NoSuchRecipe`, row untouched), `archive_of_an_absent_recipe_is_no_such_recipe`, `save_never_changes_archive_state`, `load_reports_archived_at`; bridge `archive_then_restore_round_trips_through_the_bridge`, `archived_recipes_leave_list_recipes_and_appear_in_list_archived_recipes`, `archive_of_another_households_recipe_is_storage_error`, `saving_an_archived_recipe_keeps_reporting_it_archived`; Dart `archiving hides a recipe and restoring brings it back`; widget `Delete asks for confirmation and archives on confirm` (`Archive this recipe?`, Cancel → no call, Archive → hook `h-1/r-1`, back to an empty library), `an archived recipe shows the archived marker and Restore`, `a failed archive reports the reason in a snackbar`, `the archived list restores a recipe`; post-review: bridge `list_recipes_reports_titles_ordered_by_title_not_id` (the `titles()` helper read `id`, so no test covered `RecipeSummaryDto.title` or the `ORDER BY title` key); widget `Remove ingredient drops the row and saves without it`, `Remove ingredient works while the row holds focus`; provider `a re-list failure after a write does not escape save as a write failure` / `degrades the list state instead of leaving it stale`. Manual confirmation on device: `Delete` → dialog `Archive this recipe?` with `… can be restored` → `Archive` → `Archived recipes` lists `Toast` with `Restore` → `Restore` → `Nothing archived.` → library shows `Toast` again. **Stated honestly:** no planned-meal occurrence exists before MVP-012, so the occurrence-keeps-its-reference half is discharged by proxy — `load_reports_archived_at` and the Dart archive test prove an archived id still resolves with its marker — and is named as MVP-012's to exercise on real occurrences). AC-4 PASS (`androidTapTargetGuideline`, `labeledTapTargetGuideline`, `textContrastGuideline` × light/dark for `/recipes`, `/recipes/new`, `/recipes/r-1`, `/recipes/archived`; text scale 2.0 for `/recipes`, `/recipes/new`, `/recipes/r-1`; `tab traversal reaches Add ingredient and Save on the form`; on-device uiautomator dumps read `content-desc="New recipe"`, `Archived recipes`, `Edit`, `Delete`, `Restore` and the field hints. As MVP-004 recorded: no TalkBack scripting). Gates all rc 0: `dart format --output=none --set-exit-if-changed .`, `flutter analyze`, `cargo fmt --all --check`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo test --workspace` **202/202** (was 186: food-domain 59, household-core 2, kimatta-storage 103, bridge 38), `cargo build --release` + `flutter test` **144/144** (was 95), `flutter build apk --debug`, `flutter build apk --release` (61.4 MB); `grep -rn "sqlite\|sqflite" lib/` empty (invariant 17); `git diff --stat -- rust/src/frb_generated.rs lib/src/rust` = 3 generated files only (invariant 21). **Schema v4 → v5**, one migration: `ALTER TABLE recipe ADD COLUMN archived_at TEXT CHECK (… GLOB civil date)`, nullable, no backfill (`a_migrated_recipe_is_not_archived`, `an_existing_v4_database_migrates_to_v5_without_losing_data`); six version pins bumped. **Decision gate applied** as resolved 2026-08-28: `archived_at` on `kimatta_storage::RecipeRecord` (UX lifecycle state kept out of `food_domain::Recipe`, as `HouseholdRecord::onboarded` is), `RecipeListing::{Active, Archived}`, commands `archive_recipe`/`restore_recipe`/`list_archived_recipes`/`known_unit_kinds`; `RecipeDto.archived_at` is output-only and `save_recipe` reads back after writing so an edit of an archived recipe stays archived. Dart supplies the archive date via `todayCivilDate()`; Rust reads no clock. No package added (D-031). **Deferred, named:** ingredient identity (`IngredientRefDto`) is never populated by the form — lines save with `ingredient: null`, MVP-009/MVP-011 add matching; `0–2` and `2+` amounts remain unrepresented and are rejected inline rather than silently `Unknown`; MVP-011 reuses the same marker for starter recipes. **Harness notes for the reviewer:** the form is a scrolled `Column`, not a lazy `ListView`, so off-screen controls exist for focus traversal; tests `unfocus` before `ensureVisible` because a focused `TextField` scrolls itself back after layout; the generated `RecipeDto.==` compares `lines` by List identity, so DTO assertions go field by field with the deep-list matcher. **Superseded 2026-08-29 by the orch/14 fix pass** (review `.orch/redteam-impl-handoff-orch-14-2026-08-29T0713-5c00.md`, 5 findings fixed, 2 deferred): eight tests added, so `flutter test` went 144/144 → **152/152** after `cargo build --release`. **Then superseded again by the orch/14 pass-1 review fix** (review `.orch/redteam-impl-handoff-orch-14-2026-08-29T0804-a280.md`, 3 findings fixed, 0 deferred, 0 retracted): the false test-collection claim recorded in this row was retracted and replaced with the measured breakdown above; the two stale `KNOWN_ISSUES-low.md` anchors this card moved were re-anchored; and a ninth test, `a rejected servings count scrolls its error into view`, closed the one `_firstErrorKey` dispatch arm nothing exercised — so the figure after that pass was **153/153**, rc 0. That test is a regression pin, not a fail-first test: the scroll fix is already in, and it is expected to pass. It was proved non-vacuous by mutation instead — deleting `key: _servingsKey` from the Servings field puts the error at **−899.3** against a viewport top of **56.0** and turns it red, which the one-row version the reviewer suggested would not have caught. `cargo test --workspace` stays **202/202** — no Rust source changed in either pass. `dart format`, `flutter analyze`, `cargo fmt --all -- --check` and `cargo clippy --workspace --all-targets -- -D warnings` all rc 0. Behaviour changes: a rejected Save now scrolls the first offending field back into view — the inline error was its only signal, and on the reference Pixel-5 geometry it rendered above the fold and was clipped, so the tap produced no visible change at all (`a blank title scrolls the error into view on a phone screen`, `the first of three ingredient rows scrolls its error into view`, `a row error is scrolled into view at text scale 2.0`, all at `usePixel5` rather than `useTallView`, all three failing before the fix at measured error rects 12.7, −323.3 and −1027.3 against a `[56.0, 792.7]` scroll viewport; the scroll anchors on the error `Text` itself, not the row `Card`, because at text scale 2.0 the card is 940 px against a 736.7 px viewport and aligning its leading edge would leave the error below the fold); and a whitespace-only Preparation is now rejected inline instead of passing the form and failing the whole save in Rust with no row named (`a whitespace-only preparation blocks save and names the row`, with `a padded preparation is still saved verbatim` pinning that ` sifted ` still reaches Rust untrimmed — the emit is deliberately **not** switched to `.trim()`, which would silently normalise the user's text). `RecipeLibraryNotifier.archive`/`.restore` gained their first coverage: the widget fake overrode both wholesale and re-listed with `invalidateSelf`, so gutting either real body left all 144 tests green; the three new `archive and restore refresh both lists` tests run the real bodies with only the `@protected` bridge seams replaced and assert `_republish`'s eager re-list plus the `archivedRecipesProvider` invalidation. **Evidence vintage, stated explicitly:** the device-level evidence in this row — the `uiautomator` dumps behind AC-4 and the `run-as … cat files/kimatta.db` read behind AC-2 — was captured **before** the orch/14 remediation; the automated counts and every post-review test name are **after** it. `recipe_fields.dart`, `recipe_form_screen.dart` and `recipes_provider.dart` all changed in between, so no APK has been *run on a device* against the tree these counts describe, and the HIGH fixed above is exactly the class of defect an emulator walkthrough would have caught and the tall-viewport suite could not. The two *build* gates no longer sit in that position: `flutter build apk --debug` and `flutter build apk --release` were both re-run rc 0 against the post-remediation tree at the pass-2 fix round recorded below, which is where their current vintage is stated. The gate list at the head of this row keeps its original-run figures, as the `144/144` and `202/202` counts do. **Also stated, because the row cites it as evidence:** a bare `flutter test` collects all four suites, so the 144 and the 152 figure both include the bridge Dart evidence cited in AC-1/AC-2/AC-3 above. Measured in this worktree at that pass with `flutter test --reporter json`, tallying `testDone` events per `suite` and discarding the hidden `setUpAll`/`tearDownAll` entries: `app_test.dart` **109**, `bridge_native_test.dart` **28**, `planning_cycle_test.dart` **7**, `recipe_fields_test.dart` **9** — total **153**, rc 0; `flutter test test/` gives the identical breakdown, and the three files run without the bridge suite give **125**, which is 153 − 28. (The same measurement taken before the pass-1 fix below added its one test read 108 / 152 / 124.) The human-readable reporters cannot answer "was this file collected?" at all: `flutter test` runs suites concurrently and the compact reporter coalesces the status line, so most completions are never printed with their own path — in the same bare run `recipe_fields_test.dart` is named on only 5 of its 9 tests. Naming is not collection. **Deferred this pass, both per the reviewer's own disposition:** `recipeDetailProvider` stays a non-`autoDispose` family (unbounded retention of opened recipes, bounded by library size, traded against a full-body spinner on every re-open) and Restore from the detail screen still navigates to the library while Restore from the archived list stays put — the reviewer flagged the intended landing spot as a UX call, and it was not made unattended. Both are tracked in `KNOWN_ISSUES-low.md`. **Superseded again 2026-08-29 by the orch/14 pass-2 review fix** (review `.orch/redteam-impl-handoff-orch-14-2026-08-29T0911-2a9d.md`, 2 findings fixed, 1 deferred, 0 retracted): one widget test added, so `flutter test` is now **154/154** after `cargo build --release` — re-measured by the same `--reporter json` method as above, `app_test.dart` **110**, `bridge_native_test.dart` **28**, `planning_cycle_test.dart` **7**, `recipe_fields_test.dart` **9**, total **154**, rc 0, and the three files without the bridge suite give **126**, which is 154 − 28. `cargo test --workspace` stays **202/202** — no Rust source changed in this pass either. `dart format --output=none --set-exit-if-changed .`, `flutter analyze`, `flutter build apk --debug` and `flutter build apk --release` (61.4 MB) are all rc 0 **against this tree**, which closes the currency question the review raised about the two build gates; the on-device evidence is untouched and stays pre-remediation. Behaviour change: an ingredient row's inline error no longer carries the row number it was written with — `_LineDraft.error` stores the message alone and `_row` prefixes `Ingredient N:` from the live index at paint time, so a removal renumbers every surviving message instead of leaving a card headed `Ingredient 1` reading `Ingredient 2: …` (`Remove ingredient renumbers the errors of the rows below it`, which fails pre-fix on exactly that mismatch). The rendered strings are byte-identical, so the five existing assertions on `Ingredient 1: …` still hold and no test needed updating. Clearing the survivors' errors inside `_removeLine` — the review's first suggested direction — was **rejected**: it drops rejection signals that are still true for the rows *above* the removal, which is the silent-rejection shape the pass-1 HIGH exists to prevent. **Deferred, tracked in `KNOWN_ISSUES-low.md`:** `ArchivedRecipesScreen._restore`'s silent return on a null household id — `RecipeSummaryDto` carries only `id` and `title`, so the detail screen's take-it-off-the-DTO shape is unavailable, and both remaining shapes (gating `onPressed`, or the `describeFailure` snackbar) are behaviour-preserving in every state the app can reach, because `archivedRecipesProvider.build` awaits `householdProvider` before the list renders; nothing pins them without a fake that contradicts production. Not committed or pushed. |
| 2026-08-29 | MVP-006 | Done | Orchestrated approval under `# orchestrate: auto-approve`: review `redteam-impl-handoff-orch-12-2026-08-29T0439-99c7.md`; 1 fix round(s); independent verifier `PASS` (session `bd2a243d-50b2-47a7-8548-ba5ae24aaa7a`); merged into `integration` as `1c7aa8f5b0afb62c034135f76c35834bda0eef67`. Owner ratifies at the `integration → master` merge. |
| 2026-08-29 | MVP-008 | Done | Orchestrated approval under `# orchestrate: auto-approve`: review `redteam-recipe-crud-handoff-2026-08-29T0615-043d.md`, `redteam-impl-handoff-orch-14-2026-08-29T0713-5c00.md`, `redteam-impl-handoff-orch-14-2026-08-29T0804-a280.md`, `redteam-impl-handoff-orch-14-2026-08-29T0911-2a9d.md`; 1 fix round(s); independent verifier `PASS` (session `0db42b0f-fe92-498a-95a7-ebec7b95dffc`); merged into `integration` as `2883bf53d897fbc191e622f8a930e2f6bae23461`. Owner ratifies at the `integration → master` merge. |
| 2026-08-29 | MVP-009 | Verify | AC-1 PASS (tests-first conflict matrix in `food-domain` `restriction.rs`: `every_kind_has_a_positive_and_a_negative_fixture` — one literal positive/negative row per kind, plus the documented false positive `sesame-free oil` → `sesame` asserted as a decision; `every_term_matches_its_own_kind` with hard-coded table lengths (peanuts 4, tree nuts 20, dairy 18, eggs 5, gluten 15, soy 7, fish 13, shellfish 17, sesame 2, meat 20, honey 1) and the composed diets checked against every component table; `composed_kinds_inherit_every_component_exception`; `an_exception_masks_only_its_own_window` (adversarial: `almond milk and whole milk` → dairy on the second `milk`); `matching_is_whole_token_case_folded_and_punctuation_blind` (`BUTTER, softened` ✓, `buttermilk` ✓ as its own term, `butterfly` ✗); `peanut_butter_warns_peanuts_and_not_dairy`; `almond_milk_warns_tree_nuts_and_not_dairy_but_vegan_still_sees_animal_milk`; `assessment_is_ordered_and_deterministic` (restriction-major, line-minor, stored first-seen order — a sorting implementation fails it). Bridge: `list_and_load_carry_the_same_assessment`, `assessment_is_household_scoped`, `every_read_back_is_some` (save/load/archive/restore/list-archived all carry it). Hard filter: widget `Hide known conflicts hides only conflicting recipes and says how many`, `hiding nothing shows no count line`; the chip is rendered only when some summary has `restrictions_checked > 0` (`no restrictions set shows no filter chip`, PRD §10 / invariant 19). One vocabulary, two consumers: `food_domain::assess` is the single function; MVP-023 calls the same one and cites `RULE_VERSION` (= 1)). AC-2 PASS (`other_restrictions_match_by_wording_only_and_are_listed` — `Other` matches only its own wording, multi-word phrases whole, partial wording never, and every `Other` is listed in `wording_only`; `non_ascii_names_never_panic_and_stay_unmatched` — `crème fraîche`, blank, `—`, a 10 kB name: no panic, no match, and an `Other` that tokenises to nothing matches nothing; bridge `other_restrictions_cross_as_wording_only`; widget `no restrictions set reads as unchecked, not clear` (asserts `noRestrictionsCopy` present **and** `noKnownConflictCopy` absent), `a checked recipe with nothing found is not called safe`, `wording-only restrictions are named`, `a conflict on an optional line is still shown`). AC-3 PASS (Rust `changing_the_restriction_set_changes_the_assessment_deterministically` — `[dairy]` → conflict, `[]` → `restrictions_checked 0`, `[dairy]` again `==` the first; Dart container test `a restriction save re-lists the library` on the real `RecipeLibraryNotifier` with only its bridge seam replaced — `fetches` 1 → 2 after `restrictionsProvider.notifier.save`; bridge-native `a saved recipe reports its restriction assessment and it follows the restriction set` across `saveRestrictions` → `saveRecipe`/`listRecipes`/`loadRecipe` → clear → reload). AC-4 PASS (`the_assessment_has_no_safety_flag` destructures every field — counts and lists only, `Debug` text free of `safe`/`ok`; `a_sent_assessment_is_ignored_on_save` — a request carrying a clean assessment for a conflicting line reads back with the conflict; Dart `no restriction copy claims safety` over `restrictionCopySamples` against the assurance regex; independent fresh-context copy review (haiku, read-only) returned one finding — `hiddenCountCopy`'s "Unchecked ingredients are still shown" was ambiguous — fixed to "The rest matched no known term — this is not a safety check"; the reviewer's own suggested rewrite ("Shown recipes have no known ingredient conflicts") was rejected as an assurance). Gates all rc 0: `dart format --output=none --set-exit-if-changed .`, `flutter analyze`, `cargo fmt --all --check`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo test --workspace` **223/223** (was 202: food-domain 71, household-core 2, kimatta-storage 106, bridge 44), `cargo build --release` + `flutter test` **172/172** (was 154; `app_test.dart` +14, new `restriction_warnings_test.dart` 3, `bridge_native_test.dart` +1), `flutter build apk --debug`, `flutter build apk --release`; `grep -rn "sqlite\|sqflite" lib/` empty (invariant 17); `grep -rn "::now()\|Timestamp\|Zoned" rust/crates/food-domain/src/restriction.rs` empty (invariant 20); `git diff --stat -- rust/src/frb_generated.rs lib/src/rust` lists generated files only (invariant 21); FRB accepted the nested struct-in-struct-in-enum (plan Risk 1 did not fire). **Decisions closed in scope (D-031: no package added, no schema change — the assessment is derived at read time):** option B — deterministic, case-folded, whole-token phrase matching on `IngredientLine.name` against a literal, versioned term table per `RestrictionKind`, with a nine-entry `DAIRY_EXCEPTIONS` list masked per window; `Vegetarian` = meat + fish + shellfish, `Vegan` = those + dairy + eggs + honey, composed from shared components so an exception travels to every diet that lists it; `RULE_VERSION 1`; assessment output-only on both DTOs (`RecipeDto.assessment: Option`, `None` on every request, `Some` on every read-back; `RecipeSummaryDto.assessment` required); `RecipeSummary.line_names` filled by one extra query per listing, not per recipe; filtering is Flutter view state, never stored; the filter chip lives in the body, not the app bar (text scale 2.0 on Pixel-5 width). **Scoped decision recorded (card gate "restriction taxonomy/source"):** rule version 1 matches on `IngredientLine.name` (the only populated field — identity is `NULL` on every recipe the app can create until MVP-011), not resolved catalog identity; false positives surface as explainable warnings, false negatives are bounded by copy that never claims a check found nothing wrong "safely"; MVP-023 inherits this knowingly. **Deferrals, named:** catalog-identity matching over `canonical_name` + aliases at MVP-011 (additive; same `assess`); `Other` restrictions matched by wording only and reported as such; optional lines warn like any other; non-ASCII terms unmatched (`crème` is one token); negation unreadable (`sesame-free` warns). **On-device walkthrough: NOT VERIFIED** — not run this session; the card's evidence plan names tests, not device runs, for all four ACs, so this does not block `Verify`. **Fail-first honesty:** the Rust and Dart tests for steps 1–3 and 5–7 were written alongside their implementations and could not compile against the pre-change tree (new types and required DTO fields), so their pre-fix failure is a compile failure rather than an observed assertion failure. Not committed or pushed. |
| 2026-08-29 | MVP-009 | Done | Orchestrated approval under `# orchestrate: auto-approve`: review `redteam-impl-handoff-orch-16-2026-08-29T1040-68c3.md`; 1 fix round(s); independent verifier `PASS` (session `59b44eb4-7001-4ed5-bfb9-1ac81dfa2e0b`); merged into `integration` as `ce04c2db0d8d916b53d8180f866942d82314fb6b`. Owner ratifies at the `integration → master` merge. |
| 2026-08-29 | MVP-010 | Cut | Owner decision at `docs/task/SEQUENCE.txt` step 17: cut under PRD v3 §16 ("demote before cutting controller proof" — photos rank last and nothing contends). Basis: no code or schema references photos; no later card assumes them; MVP-011 packages text-only starter content. MVP-022 dependency cell amended (card + register); MVP-011 promoted `Draft → Ready`. Reversible as a post-launch card. |

### PRE-001 command contract

Pinned versions: Flutter 3.47.1 · Dart 3.13.1 · JDK 17 (WSL 17.0.19, Windows 17.0.20.1) · Gradle 9.3.1 · Android platform-36 · build-tools 36.1.0 · platform-tools 37.0.1 · cmdline-tools 15859902 · emulator 37.1.11 · `system-images;android-36;google_apis;x86_64` rev 7 · AVD `pre001_avd` (device `pixel_5`, Android 16 / API 36).

WSL locations: Flutter `~/development/flutter`, Android SDK `~/Android/sdk`, `JAVA_HOME=/usr/lib/jvm/java-17-openjdk-amd64`. Windows: Android SDK `C:\Android\sdk`, JDK `C:\Program Files\Microsoft\jdk-17.0.20.101-hotspot`.

```bash
flutter create --org dev.mealmate --project-name meal_mate <project>
dart format --output=none --set-exit-if-changed .
flutter analyze
flutter test
flutter build apk --debug
```

`--org dev.mealmate --project-name meal_mate` sets the Dart package to `meal_mate` (matching DEC-001) but also assembles the applicationId as `dev.mealmate.meal_mate` — Flutter has no flag to decouple the applicationId's final segment from the project name (verified against `create_base.dart`'s `createAndroidIdentifier`, which returns `$organization.$name`). MVP-001 must explicitly set `applicationId "dev.mealmate.temp"` in `android/app/build.gradle` (and the corresponding package path) after scaffolding to reach DEC-001's actual identity — this command only gets the Dart package name and org prefix right. The explicit `minSdk 24` pin (D-020) is also Gradle work for MVP-001, not this command — PRE-001 only verified the template default already equals 24. Identifier flags were not exercised by PRE-001; MVP-001 AC-2 remains the point of verification, not this contract.

Emulator + adb bridge (D-022 shape — emulator on the Windows host, WSL as adb TCP client):

```bash
# 1. Launch the emulator (Windows host, non-blocking)
powershell.exe -NoProfile -Command "Start-Process 'C:\Android\sdk\emulator\emulator.exe' -ArgumentList '-avd','pre001_avd'"

# 2. Verify hardware acceleration before waiting on boot -- assert exit code, not stdout text
powershell.exe -NoProfile -Command "& 'C:\Android\sdk\emulator\emulator.exe' -accel-check; exit $LASTEXITCODE"
# If -accel-check fails (non-zero exit), the emulator from step 1 is already running with no
# adb bridge yet -- kill it directly instead of relying on the adb-based teardown in step 7:
#   taskkill /IM emulator.exe /F
#   taskkill /IM qemu-system-x86_64.exe /F   # the front-end always exists here; the qemu kill
#                                            # is a no-op if no child was spawned

# 3. Windows-side adb server bound to all interfaces
powershell.exe -NoProfile -Command "Start-Process -WindowStyle Hidden 'C:\Users\David\AppData\Local\Microsoft\WinGet\Packages\Google.PlatformTools_Microsoft.Winget.Source_8wekyb3d8bbwe\platform-tools\adb.exe' -ArgumentList '-a','-P','5037','nodaemon','server'"
# NOTE: this is the winget-installed adb.exe -- distinct from C:\Android\sdk\platform-tools\adb.exe,
# which the emulator's SDK-root validation requires to exist (prerequisite 3) but which this bridge
# does not itself run. The "Users\David" segment is host-specific; re-derive with `where.exe adb.exe`.

# 4. WSL client points at it via the default-route gateway
export ADB_SERVER_SOCKET=tcp:$(ip route | awk '/default/ {print $3}' | head -1):5037

# 5. Wait for boot to complete before installing (bounded -- an emulator that never boots
#    otherwise hangs the contract indefinitely; on timeout, treat as accel/boot failure and
#    fall back to the taskkill teardown noted above)
timeout 120 adb wait-for-device
for i in $(seq 1 60); do
  [[ "$(adb shell getprop sys.boot_completed 2>/dev/null | tr -d '\r')" == "1" ]] && break
  sleep 2
done

# 6. Install and launch
adb devices && adb install -r <apk> && adb shell monkey -p <applicationId> -c android.intent.category.LAUNCHER 1

# 7. Teardown when the session ends
adb emu kill
adb kill-server
```

Operational entry point since 2026-08-25: `tools/emulator.sh` (D-033).

**Prerequisites discovered during PRE-001, required for MVP-003/004/005:**

1. Windows Firewall had two inbound **Block** rules named `adb.exe` (program-scoped, `ports=Any`). Block overrides Allow, so no port-based Allow rule can work while they exist. They must be removed (elevated) or the WSL→emulator bridge silently times out.
2. An inbound Allow rule for TCP 5037 is required (elevated): `New-NetFirewallRule -DisplayName 'WSL adb server' -Direction Inbound -Protocol TCP -LocalPort 5037 -Action Allow -Profile Any -RemoteAddress 172.21.80.0/20`. Close it when emulator work is done (elevated): `Remove-NetFirewallRule -DisplayName 'WSL adb server'`.
3. Windows `platform-tools` must be installed under `C:\Android\sdk` even though the bridge uses a different `adb.exe` — the emulator validates the SDK root and aborts with `Broken AVD system path` without it.
4. `systeminfo` is not a usable WHPX signal on a WSL2 host (it reports only "A hypervisor has been detected"). Use `emulator.exe -accel-check` and assert on its **exit code**; its stdout puts `accel:` and the code on separate lines.

### DEC-002 command contract

Extends the PRE-001 contract above. Required for MVP-002 onward.

```bash
dart format --output=none --set-exit-if-changed .
flutter analyze
(cd rust && cargo fmt --all --check && cargo clippy --workspace --all-targets -- -D warnings && cargo test --workspace)
(cd rust && cargo build --release) && flutter test   # stale .so = false pass; never run alone
flutter build apk --debug
```

The `lib/domain` coverage gate (`lcov`/`genhtml`) and the domain-independence grep that stood here were removed under D-028 — durable domain code now lives in Rust, and MVP-002 added the Rust gate above (2026-08-24). `lib/features` coverage reporting is optional and non-gating; re-add `--coverage` when a card asks for it. The `glados` property-test targets named below (unit conversion, shopping aggregation, restriction matching, planning-cycle date math) are now Rust functions — property tests for them belong to MVP-025 in Rust; `glados` remains available for any Dart-side logic that warrants it.

### PRE-002 command contract

Extends the DEC-002 contract above. Required for MVP-002 onward. Backend: **native-assets** (`flutter_rust_bridge_codegen integrate --integration-backend native-assets`) — Flutter's own build-hook pipeline (`hook/build.dart` → `flutter_rust_bridge_hooks` → `native_toolchain_rust`) compiles and bundles the Rust crate; no Gradle plugin, no `cargo-ndk`. Chosen because the card asked for it first and it passed within budget (1 of 2 fixes used; cargokit not attempted). Pinned: Rust 1.98.0 (`rust/rust-toolchain.toml`, channel + targets), `flutter_rust_bridge` 2.13.0 = `flutter_rust_bridge_hooks` 2.13.0 = codegen 2.13.0 (exact pins in `pubspec.yaml`, `=2.13.0` in `rust/Cargo.toml`). Installs live in `~/.cargo` and `~/.rustup` (user-level; agent shells must `export PATH="$HOME/.cargo/bin:$PATH"`). NDK 28.2.13676358 is discovered by Flutter (`ndkVersion` default); nothing to configure.

```bash
# install (once per host)
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y --no-modify-path --default-toolchain 1.98.0 --profile minimal
export PATH="$HOME/.cargo/bin:$PATH"
rustup target add --toolchain 1.98.0 aarch64-linux-android armv7-linux-androideabi x86_64-linux-android
rustup component add --toolchain 1.98.0 rustfmt clippy
cargo install flutter_rust_bridge_codegen --version 2.13.0 --locked

# generate bindings after any change under rust/src/api (writes lib/src/rust/** and rust/src/frb_generated.rs — never hand-edit)
flutter_rust_bridge_codegen generate

# gate + build. The generated loader reads rust/target/release/; the native-assets hook
# builds under .dart_tool/hooks_runner and never refreshes it — a stale .so gives a FALSE
# PASS, not a load failure, so never run `flutter test` alone after a Rust edit.
dart format --output=none --set-exit-if-changed .
flutter analyze
(cd rust && cargo fmt --all --check && cargo clippy --workspace --all-targets -- -D warnings && cargo test --workspace)
(cd rust && cargo build --release) && flutter test
flutter build apk --debug
flutter build apk --release
```

- **ABI set:** `arm64-v8a`, `armeabi-v7a`, `x86_64` — `libkimatta_bridge.so` present in each, in both debug and release APKs.
- **Dev cycle (AC-7):** Rust edit → `generate` → `flutter build apk --debug` = 11.05 s, 10.32 s, 9.33 s (three runs, warm Gradle). The hook always builds the cargo `release` profile, debug APKs included.
- **Test↔toolchain coupling (DEC-005 decision 3):** `flutter test` runs the build hook, so every Dart test — the fake-fed `test/app_test.dart` included — needs rustup + the pinned toolchain on the host; `test/app_test.dart` avoids *loading* the library, not *building* it. Additionally the generated loader looks in `rust/target/release/` under `flutter test`, hence the `&&`-chained `cargo build --release` command above (recorded as fix #1 in the PRE-002 ledger; no generated or integrate-written file was edited).
- **Layout deviation from the card:** the bridge crate is `rust/` itself (`kimatta_bridge`, FRB's default), not `rust/crates/kimatta-bridge`; cargo rejects an empty `crates/*` workspace glob, so MVP-002 adds the `[workspace]` block when the first sibling crate exists. `--no-integration-test` was passed to `integrate`.
- **Generation tooling:** data-carrying error enums become `freezed` sealed classes (`KimattaError implements FrbException`; variants `KimattaError_InvalidPath`, `KimattaError_Storage`), so `freezed` 4.0.0 / `build_runner` 2.16.0 (dev) and `freezed_annotation` 3.1.0 were added. They serve generated bindings only (invariant 21); D-024's no-codegen rule still governs hand-written Dart.
- Emulator install/launch/teardown: PRE-001 contract steps 3–7. Note from this run: `adb emu kill` cannot reach the emulator console from WSL; close the emulator on the host (`taskkill /IM qemu-system-x86_64.exe /F` (works whether the AVD was launched by emulator.exe or Android Studio; see tools/emulator.sh)).

## Calibration and post-launch

After the first three implementation cards (`MVP-001` through `MVP-003`), review actual duration, context load, test yield, and remediation count; resize later cards only if evidence warrants it.

**First-three-card sizing calibration (recorded 2026-08-28, at `MVP-003`'s handoff).** Measured per card: `MVP-001` (Focused/Standard) — one working session, 1 test carried over from the scaffold, 0 remediation cycles, though it needed a fresh-context verifier pass and an owner confirmation for the permission-blocked APK path. `MVP-002` (Focused/Elevated) — spanned 2026-08-24 to the 2026-08-27 closeout with on-device evidence captured 2026-08-25, 9 Rust tests plus 4 bridge tests, 0 remediation cycles, but it reached `Done` without the fresh-context verifier its Elevated tier names, and its closeout left prose residue that `MVP-003` step 1c had to sweep. `MVP-003` (Focused/Standard) — one working session, 14 new widget tests (18 total with the bridge suite), 1 remediation cycle (six findings from the orchestrated red-team pass, fixed before independent verification), 2 new packages, one emulator evidence cycle that passed first try. **Verdict: do not resize.** All three fit a single session's implementation once their plan existed, and the two cards that ran long did so in *planning and doc reconciliation*, not implementation — `MVP-003`'s plan reached rev 11 across several red-team passes while its code is six small files. The lever that would actually shorten these cards is fewer concurrent writers on the governance docs and a shorter plan-revision loop, not smaller cards. Two process findings carry forward: an Elevated card must not reach `Done` without its verifier, and a card's closeout should discharge its own prose residue rather than leaving it for the next card's step 1.

Post-launch begins with iOS feasibility/tooling and the iOS client, then follows PRD phases 1.5, 2, 2.5, and 3. Other candidates include improved recommendation calibration, richer collaboration, dedicated local storage only if measured need emerges, deterministic imports beyond OPT-001, paid delegation/LLM conveniences, and desktop experiments. Promote a candidate only by adding a bounded card and dependencies here.
| 2026-08-29 | MVP-011 | Verify | Content: **10 recipes authored / 0 installed** (all `"basis": "original"`, all `cook_review: null`) and **37 catalog ingredients** with `store_category`, seeded unconditionally. Exclusions: `cc_by`, `cc_by_sa`, `cc_nc`, `cc_nd` and scraped material — listed in the file's `policy.excluded`, and `a_cc_by_basis_is_rejected` makes the excluded case **unrepresentable**, not merely absent. No images packaged (`MVP-010` cut 2026-08-29; `grep -rn 'image\|photo' lib/ rust/src rust/crates` over `.dart`/`.rs` returns **0** outside the generated bindings). AC-1 PASS (`starter_recipes.json` is simultaneously the shipped data and the provenance manifest, so the two cannot drift; `every_rights_basis_is_in_the_allowed_set` hard-codes the allowed trio rather than mirroring `RightsBasis::ALL`; `every_verified_on_is_a_civil_date`, `every_slug_is_unique`, `every_catalog_id_is_unique`, `every_catalog_reference_resolves`). AC-2 PASS (`rights_and_slug_round_trip`, `prep_minutes_round_trips_and_absence_stays_absent`, `prep_minutes_survives_save_load_and_the_bridge`; `every_recipe_matches_its_declared_conflicts` builds each entry as a `Recipe` and asserts `food_domain::assess` against all 11 `RestrictionKind`s **equals** the declared set; `an_unknown_unit_token_is_rejected` and `a_zero_quantity_is_rejected` pin the unit/quantity checks; Dart `starter content installs once and is idempotent`, `installed starter content survives reopening the same file`). **AC-3 NOT VERIFIED** — nothing has been cooked. The 10 pending slugs are `sheet-pan-lemon-chicken`, `red-lentil-soup`, `black-bean-tacos`, `creamy-tomato-spaghetti`, `peanut-noodle-bowl`, `baked-salmon-and-rice`, `vegetable-fried-rice`, `coconut-chickpea-curry`, `cheese-and-bean-quesadillas`, `garlic-shrimp-spaghetti`. The resolving action is the owner's cook log: fill the `cook_review` object (`cooked_on`, `by`, `corrections`) in `rust/crates/food-domain/content/starter_recipes.json` and rebuild — no code change is needed, pinned by `a_recorded_cook_review_moves_an_entry_into_the_shipped_set`. This is **not** a D-027 owner-acceptance case — cooking is inside this card's own scope — so it **blocks `Done`**. AC-4 PASS (licence inventory is the `policy` block plus the per-entry `rights` object; no image path exists anywhere in the tree). **Schema v5 -> v6**, one migration: `prep_minutes` on `recipe` (CHECK >= 1) and `rights_basis`/`attribution`/`modifications`/`verified_on` (civil-date GLOB)/`starter_slug` on `recipe_provenance`, plus an index on `starter_slug`. All nullable, **no backfill** (`a_migrated_recipe_has_no_prep_estimate_and_no_rights`); migrations tested v1/v2/v3/v4/v5 -> v6. The `rights_basis`/`verified_on` pairing is not expressible through `ALTER TABLE ADD COLUMN`, so it is enforced on the read path where migration 5 already puts date correctness: rights are present **iff** both are non-NULL, and every other shape is `CorruptRights`, never coercion (`a_partial_rights_row_is_reported_not_coerced` and its mirror, `an_attribution_without_a_basis_is_reported_not_dropped`, `a_glob_passing_non_date_verified_on_is_reported` for the GLOB-passing `2026-13-45`, `a_corrupt_rights_basis_row_is_reported_not_coerced`). The four rights columns move as **one group**: a re-save carrying no rights preserves all four (`editing_a_starter_recipe_keeps_its_rights_and_slug` — the MVP-008 edit path, whose DTO rights scalars are output-only), and a re-save carrying rights overwrites all four, so an absent attribution clears rather than silently retains the old credit (`an_edit_supplying_rights_with_no_attribution_clears_the_stored_one`). `a_sent_rights_basis_is_ignored_on_save` (adversarial, mirroring `a_sent_assessment_is_ignored_on_save`) pins that a Dart caller cannot forge a rights basis or mint itself a starter slug. **Packages added, D-031 in-scope (card evidence, not a Draft event):** `serde` 1 (derive) and `serde_json` 1 on `food-domain`, both named in PRD v3 §6.6. **Limitations, stated rather than hidden:** (1) **install-once** — `install_starter_content` is insert-only, so a revised recipe never reaches a device that already holds the slug (`an_edited_installed_starter_recipe_is_not_overwritten`), and a catalog correction carrying no new id never lands either, because the read-only short-circuit fires first; correcting installed content is a later card's work and the next content card must not assume propagation works; (2) **idempotency is code-enforced, not schema-enforced** — `recipe_provenance` has no `household_id`, so a partial unique index on `(household_id, starter_slug)` is not expressible without touching `recipe`, which MVP-007's deferral asked to avoid; the single process-wide connection mutex makes a concurrent install unreachable, and the slug check shares one transaction with the writes; (3) `prep_minutes` **does** touch `recipe`, which MVP-007 AC-4 asked the *rights* columns to avoid — the rights columns still land on `recipe_provenance`, and `prep_minutes` is a distinct addition justified by this card's own shippability constraint (a starter recipe that cannot be filtered on prep time is not shippable) and by MVP-023 AC-1's prep-window hard filter; (4) **first `#[allow]` in the Rust workspace** — `Recipe::new` now takes 8 arguments and trips `clippy::too_many_arguments`; it is a whole-aggregate constructor whose parameter list *is* the struct, so the allow carries a written reason and grouping the fields is left to whoever next adds one rather than done across 11 call sites here. **Content decision recorded:** `expected_conflicts` records what `assess` actually returns under `RULE_VERSION` 1, not a nutritional claim. `peanut-noodle-bowl` therefore declares `gluten`: rule version 1 matches the term `noodles` inside `rice noodles`, which are gluten-free. The declaration was kept honest rather than dodged by renaming the ingredient — MVP-009 already accepted explainable false positives, over-warning is the safe direction, and the warning copy never claims safety. Recorded in the file's `policy.expected_conflicts_note`. **The 'shipped' ambiguity, surfaced rather than decided silently:** AC-3 says 'every shipped recipe has a recorded cook review'. Under *installed into a household* the strict gate satisfies it — nothing unreviewed installs, proved on device below. Under *present in the release artifact* the 10 authored entries are embedded in the APK either way, so no option that authors them in this card satisfies AC-3 — and the card's own Scope requires authoring here, which would make the card self-contradictory. This plan used the first reading. The owner's third option remains open: **option C — ship the mechanism now and author the roster in a follow-up card.** Because the install path is re-runnable, recording cook reviews later and rebuilding delivers the roster to existing devices; the roster is deferred, not stranded. **Two narrowings routed here for ratification (Step 1 card note):** CC BY excluded as an uncertain rights case under the card's own **Decision gates** (attribution survival is `MVP-020`'s unbuilt work); shipped candidate set **10** rather than 30 under 'quantity targets may shrink rather than lower quality' and PRD v3 §16. If the owner reads D-031 differently, the card returns to Draft at ratification. **Gates, all rc 0, each redirected to a file with `$?` checked and none piped through a filter:** `dart format --output=none --set-exit-if-changed .`, `flutter analyze`, `cargo fmt --all --check`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo test --workspace` **288** (food-domain 98, household-core 2, kimatta-storage 134, bridge 54), `cargo build --release` + `flutter test` **186/186**, `flutter build apk --debug`, `flutter build apk --release` (62.5 MB). **Baseline correction:** the pre-change tree measured **226** Rust / **175** Dart, not the 223/172 this register records against `MVP-009` — those figures were written into the `MVP-009` `Verify` row *before* its orch/16 fix round landed (+1 food-domain, +2 bridge). Invariants: 17 — `grep -rn 'sqlite\|sqflite' lib/` empty; 21 — `git diff --stat -- rust/src/frb_generated.rs lib/src/rust` lists 5 generated files only, plus the new generated `lib/src/rust/api/starter.dart`, all produced by `flutter_rust_bridge_codegen generate` and never hand-edited; `cargo tree -i rusqlite` one version. **On-device evidence, `emulator-5554`, AVD `pre001_avd`, API 36, debug APK — two runs, and the second is the one that matters.** *Run A, fresh (`pm clear`, install, launch):* `PRAGMA user_version` **6**, `integrity_check` **ok**, `SELECT count(*) FROM ingredient` **37** and `ingredient_alias` **27** (catalog seeded), `SELECT count(*) FROM recipe r JOIN recipe_provenance p ON p.recipe_id = r.id WHERE p.starter_slug IS NOT NULL` **0** (nothing unreviewed installed), `adb logcat -s AndroidRuntime` **0** `FATAL EXCEPTION`; force-stop and relaunch re-read 37/27/0 unchanged, so the re-run duplicates nothing. *Run B, upgrade path, no `pm clear`:* the schema-v5 fixture captured at Step 0(3) from the **baseline** APK (126,976 B, matching the device's `stat`; `user_version` 5, one household, one recipe `Baseline Fixture Recipe` saved through the real UI) was pushed back to `files/kimatta.db` and confirmed at `user_version` **5** with `ingredient` **0** on the device before launch; after launching the new build, `user_version` **6**, `integrity_check` **ok**, the household intact, the pre-existing recipe intact with `servings` and `prep_minutes` both NULL (no backfill), **catalog seeded anyway at 37/27**, and **0** `FATAL EXCEPTION`. That last assertion is the case an onboarding-wired install would have failed silently: the device was already onboarded, so `welcomeLocation` was unreachable. **Step 10(1) shape:** the install is a latched once-per-launch call in the existing `ref.listen(householdProvider, …)` lifetime subscription in `lib/app/app.dart`, **not** on the onboarding path; `_installStarted` sits beside the existing `_gated` because `HouseholdNotifier.rename`/`.completeOnboarding` both republish `AsyncData`; and `MaterialApp.router` gained a `scaffoldMessengerKey` because `_AppState`'s context sits *above* `MaterialApp` where `ScaffoldMessenger.of(context)` throws. Pinned by `starter content installs once per launch for an already-onboarded household`, `renaming the household does not re-run the starter install`, `the install is not called before the household resolves`, and `a failed starter install does not block the app`. **Both pins were proved non-vacuous by mutation:** deleting the `_installStarted` latch and the `scaffoldMessengerKey` fails exactly those two tests and no others. `harness()` gained an unconditional `starterInstall:` override for the reason its in-file comment already gives — `flutter test` never calls `RustLib.init()`. **Fail-first honesty:** the Rust tests for steps 2-9 reference types that do not exist before their own step, so their pre-fix failure is a compile failure rather than an observed assertion failure — the same limit `MVP-009`'s row recorded. Three tests did fail first for real: `every_recipe_matches_its_declared_conflicts` rejected the declared conflicts of three entries until they were corrected to what `assess` actually returns, and the two mutation-checked widget tests above. **Next implementation task: none** — `MVP-011` is at `Verify` pending the owner cook log for AC-3. `docs/task/README.md` planning-gate check 3 forbids planning another implementation card while this one sits at `In Progress`/`Verify`, and the carve-out covers only owner-paced decision cards like `DEC-004`, so `SEQUENCE.txt` step 22 is blocked until the owner either approves `MVP-011` `Done` or records a decision that its `Verify` is content-only and does not occupy the lane. `MVP-012` was deliberately **not** promoted, because doing so would create exactly the deadlock the execution gate exists to detect. Not committed or pushed. |
| 2026-08-29 | MVP-011 | Done | Orchestrated approval under `# orchestrate: auto-approve`: review `redteam-impl-handoff-orch-21-2026-08-29T1336-81e8.md`; 1 fix round(s); independent verifier `NOT_VERIFIED` (session `427d4899-f7c8-435f-85fb-7cb5d38317b0`); merged into `integration` as `887c3b3e5d41777df358d1a86e002562b8c3828d`. Owner ratifies at the `integration → master` merge. AC-3 stands `NOT VERIFIED` under the D-027 exception, owner-accepted 2026-08-29: (1) the resolving action is a human cook log, not the card's own work; (2) it is discharged by recording `cook_review` entries in `rust/crates/food-domain/content/starter_recipes.json` before `MVP-022` beta readiness — tracked in `KNOWN_ISSUES.md`; (3) residual risk accepted: the beta ships whatever subset has been cooked (0/10 today), and `shipped_starter_content()` guarantees nothing unreviewed installs. External actions (read-only source research; no scraped/licensed content) confirmed satisfied: all ten entries are original authorship. |
| 2026-08-29 | MVP-012 | Verify | **Schema v6 → v7**, one migration: `planned_meal` (`id`, `household_id` FK cascade, `date` civil-date GLOB, `slot`, `locked` 0/1 default 0, `UNIQUE (household_id, date, slot)`, indexed `(household_id, date)`) and `meal_component` keyed `(planned_meal_id, position)` with `kind`, nullable `recipe_id` FK `NO ACTION` + index, `note`, `scale_numer`/`scale_denom`, and CHECKs pinning the kind/column pairing. Eight version pins bumped (seven `…_to_v7…` Rust pins incl. `reopen_is_idempotent`, plus `bridge_native_test.dart` `schema v7`); new `an_existing_v6_database_migrates_to_v7_without_losing_data`, `the_component_recipe_foreign_key_is_indexed` (absent-at-6/present-at-7 form), `a_second_occurrence_in_the_same_slot_is_refused_by_the_unique_constraint`, `a_recipe_component_without_a_recipe_id_is_refused_by_the_check`, `a_half_present_scale_is_refused_by_the_check`, `a_scaled_non_recipe_component_is_refused_by_the_check`. AC-1 PASS (`kimatta-storage` `a_multi_component_occurrence_round_trips_by_date_and_slot` — recipe 3/2 + as-written recipe + leftovers, load `==` saved; `every_non_recipe_kind_round_trips` — leftovers/dining_out/frozen_quick/freeform notes verbatim, `open` alone, five `recipe_id IS NULL` rows all carrying a kind token; `list_returns_an_inclusive_date_range_in_date_then_slot_order`, `list_of_an_empty_range_is_empty`; bridge `save_then_load_returns_an_equal_dto`, `list_orders_by_date_then_slot`; Dart `a planned meal with two components survives reopening the same file`). AC-2 PASS (`food-domain` `move_component_reorders_and_rejects_out_of_range`, `remove_of_the_last_component_is_refused`, `set_scale_on_a_non_recipe_component_is_refused`, `a_zero_scale_is_rejected_and_none_means_as_written`, `freeform_needs_a_non_blank_note_kept_verbatim`, `open_must_be_the_only_component`, `a_meal_needs_at_least_one_component`, `component_kind_tokens_round_trip_and_reject_unknown` (hard-coded six), `a_mis_shaped_row_is_none_not_coerced`; storage `save_replaces_the_whole_component_set_and_keeps_order` (reorder + re-scale survive a re-save), `scales_are_stored_in_lowest_terms` (2/4 → 1/2), `a_corrupt_component_row_is_reported_with_its_coordinates` (via `PRAGMA ignore_check_constraints`, so the reader's own refusal is what is proved), `an_unknown_kind_row_is_rejected_on_read`; bridge `a_zero_scale_is_a_typed_planned_meal_error`, `an_unknown_kind_token_is_a_typed_planned_meal_error_and_writes_nothing`, `a_non_civil_date_is_a_typed_planning_error`, `known_kinds_lists_every_kind`). AC-3 PASS (`WriteSource { User, Automation }` contract enforced in storage: `a_user_write_to_a_locked_occurrence_succeeds`, `an_automation_write_to_a_locked_occurrence_is_refused_and_writes_nothing` (component swap and delete both `LockedPlannedMeal`, row + 3 components unchanged), `an_automation_write_to_an_unlocked_occurrence_succeeds`, `unlock_then_automation_write_succeeds`, `automation_may_create_an_occurrence_in_a_free_slot`, `lock_is_idempotent_and_scoped`, `automation_cannot_change_lock_state` (set and clear both refused — closes the unlock-then-write bypass), `save_never_changes_lock_state` (asserts the **re-read** value), `a_new_occurrence_is_never_locked_on_insert`, `delete_removes_the_occurrence_and_its_components`; domain `lock_is_a_plain_boolean_with_no_weight`; bridge `a_sent_locked_flag_is_ignored_on_save` (adversarial), `set_lock_then_save_keeps_it_locked`; Dart `a locked planned meal stays locked across save`). AC-4 PASS (`planned_meal_reads_and_writes_are_household_scoped` — h2 cannot load, list, save-over, lock or delete h1's occurrence and h1's value is equal after every attempt; `a_component_naming_another_households_recipe_is_rejected_before_any_write` (`ComponentRecipeMismatch`, and `NoSuchRecipe` for a ghost); `occurrence_for_an_absent_household_is_rejected`; `deleting_a_household_cascades_to_its_planned_meals`; bridge `commands_are_scoped_to_the_named_household`; Dart `a planned meal id from a different household is not found`, `a PlannedMeal error is a typed KimattaError_PlannedMeal`; fresh-context read-only schema review (haiku) against MVP-013/015/023: every card joins by FK with no parallel array, household scope is one `planned_meal.household_id` predicate for every read, no non-additive gap named — PASS). Also `an_archived_recipe_is_still_a_valid_component` discharges the half of MVP-008's archive policy that had only a proxy; `a_slot_outside_the_enabled_scope_is_rejected_on_write` + `disabling_a_slot_later_keeps_existing_occurrences_readable` pin both halves of the write-time slot check; `a_household_with_no_cycle_cannot_plan_yet` (`NoPlanningCycle`, distinct from `SlotNotEnabled`); `saving_a_new_id_into_an_occupied_slot_is_occupied_slot`. Widget `Settings reports a planned-meal failure in prose` pins the unconditional `KimattaError_PlannedMeal` arm in `describeFailure`. Gates all rc 0, each run as a single invocation with its exit status read from the tool result: `dart format --output=none --set-exit-if-changed .`, `flutter analyze`, `cargo fmt --all --check`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo test --workspace` **345** (was **291** measured at Step 0: food-domain 100→112, household-core 2, kimatta-storage 134→167, bridge 55→64 — +54, exactly the tests added), `cargo build --release` + `flutter test` **All tests passed** (Step 0 baseline **187**, not the 179 the plan recorded; +5 added: one widget, four bridge-native), `flutter build apk --debug`, `flutter build apk --release` (62.7 MB); `cargo tree --workspace -i rusqlite` one version (0.40.2). Invariants: 5 — occurrence, date/slot, components and per-component scale are four distinct concepts in the schema; 17 — `grep -rn "sqlite\|sqflite" lib/` empty, only Rust writes the two new tables; 18 — the lock is a storage guarantee keyed on `WriteSource`, no numeric conversion exists; 19 — no cooked/status column, a `planned_meal` row asserts planning only (migration comment + domain doc); 20 — `grep -rn "::now()\|Timestamp\|Zoned"` over `planned_meal.rs`/`api/planned_meals.rs` empty, every read is a pure function of stored rows; 21 — six coarse bridge commands, `git diff --stat -- rust/src/frb_generated.rs lib/src/rust` lists 8 generated files plus the new generated `lib/src/rust/api/planned_meals.dart`, all from `flutter_rust_bridge_codegen generate` + `build_runner`, never hand-edited. Stop conditions checked: no `recipe_id` on `planned_meal`, `meal_component.kind NOT NULL`, `open` is a kind token not a NULL recipe (`every_non_recipe_kind_round_trips`). **Decisions closed in scope (D-031: no package added):** per-**component** `kind` (v2 `MealComponent.type` shape) over a per-occurrence kind, so mixed meals exist and SQL CHECKs carry the recipe/kind pairing; `MealStub` = MVP-007's line-less `Recipe`, no `meal_stub` table (additive later); one occurrence per `(household, date, slot)` (UNIQUE, reversible by migration); no cooked/status column (ledger owns confirmation, MVP-023/024); scale = multiplier `Option<Rational>`, `None` = 1×, because `Recipe::servings` is nullable; lock enforced in storage via `WriteSource`, and automation may neither set nor clear one; slot-enabled check on write only; component identity = position (house pattern; a reorder changes it). **Deviations from the plan, stated:** `MealComponent::parse_scale(numer, denom)` maps both zero halves to `ZeroScale` rather than a constructor rejecting `numer == 0` — a `Rational` cannot be zero, so the plan's constructor check was unreachable; optional non-freeform notes are stored verbatim with no blank check (the plan's error vocabulary has none); `parse_row` returns `Ok(None)` for a kind/column mismatch, which storage reports as `CorruptComponent` with coordinates and the bridge as a typed `PlannedMeal` input error. **Deferred, named for MVP-013/015/023/024:** no stable per-component id (reference occurrences, not components); no undated "unplanned this cycle" bucket; no "takeout if enabled" household flag (an MVP-023 policy input); no leftovers→source-occurrence link (additive nullable column if needed); no cooked/confirmed vocabulary; MVP-013 must read the planning provider (`ensure_planning_cycle`) before its first save or `NoPlanningCycle` fires. **Honest limits:** no on-device run — the card's evidence plan names tests for all four ACs; Rust tests for new types are compile-fail-first (as MVP-009/011 recorded) except the seven `== 7` pins, which were observed failing `6 != 7` before the migration landed; the bridge-native Dart tests are expected-to-pass pins, labelled as such. Not committed or pushed. |
| 2026-08-29 | MVP-012 | Done | Orchestrated approval under `# orchestrate: auto-approve`: review `redteam-impl-handoff-orch-23-2026-08-29T1512-79d5.md`; 1 fix round(s); independent verifier `PASS` (session `561ffffb-0457-474b-8457-75dc4e19fe04`); merged into `integration` as `bf409a5d3cb8850d375f00abf129de6ab9904869`. Owner ratifies at the `integration → master` merge. |
| 2026-08-29 | MVP-014 | Verify | **Model decision, inherited by `MVP-015`/`MVP-016`/`MVP-023`: Option A, row-presence.** A `pantry_item` row means *this household marked this identity as one it has*; **no row means no record, which is unknown — never "they do not have it"** (invariant 6, card constraint, PRD §10). There is deliberately **no `present` column**: a stored `0` would be exactly the absence claim the model may not make, and every consumer acts identically on "no record". The boolean is named **`marked`, not `present`, at every layer that leaves the storage crate** (`PantryEntry.marked`, `PantryEntryDto.marked`) so no downstream card reads `false` as evidence of absence. Tri-state (`present INTEGER NOT NULL`) was rejected as literally the third state the card's "State is binary have/don't-have" line forbids; a JSON array on `household` was rejected under PRD v3 §12; replace-the-whole-set (the `save_restrictions` shape) was rejected because the pantry is 37 catalog identities plus every custom ingredient edited one tap at a time — `set_planned_meal_lock` is the in-repo precedent that a targeted boolean toggle is still a service-level command (invariant 21). Identity is the existing `food_domain::IngredientRef`; no new identity concept, so "identity matching is explicit" holds by construction — a recipe line either resolves to a ref or gets no control. **Schema v7 → v8**, one migration: `pantry_item(household_id FK cascade, ingredient_id FK NO ACTION, custom_ingredient_id FK NO ACTION, CHECK ((ingredient_id IS NULL) <> (custom_ingredient_id IS NULL))) STRICT`, keyed by **two partial unique indexes** (`pantry_item_catalog`, `pantry_item_custom`) because either column is NULL half the time so no PRIMARY KEY can carry it, plus a plain index per FK because the household-leading unique indexes cannot serve an FK-parent-delete lookup. Sixteen version pins bumped (eight Rust `schema_version` assertions, seven Rust test *names* `…_to_v7…`→`…_to_v8…` incl. `empty_db_migrates_to_v8`, and `bridge_native_test.dart`'s `schema v8` name + literal); `test/app_test.dart:26`'s `schemaVersion: 5` is a hand-written fake, not a pin, and was left alone. New schema tests: `an_existing_v7_database_migrates_to_v8_without_losing_data`, `a_pantry_row_naming_both_or_neither_ingredient_kinds_is_rejected` (adversarial raw INSERTs; the CHECK fires before FK enforcement), `the_pantry_identity_indexes_reject_a_duplicate_row` (adversarial — duplicate refused for both kinds **and** the same ingredient in a *second* household accepted, so a silently ineffective index fails), `the_pantry_foreign_keys_are_indexed` (absent-at-7/present-at-8 form), `deleting_a_household_cascades_to_its_pantry_items` (expected-to-pass, labelled: `pantry_item` is the first table that is simultaneously a cascading child of `household` and a `NO ACTION` child of `custom_ingredient`, itself a cascading child of `household`, so one delete exercises both edges). **AC-1 PASS** — `kimatta-storage` `marking_an_identity_persists_and_unmarking_removes_it` (catalog and custom), `marking_and_unmarking_are_both_idempotent` (two marks → one row; two unmarks → zero), `pantry_state_persists_across_reopen`; bridge `marking_and_unmarking_round_trips_through_the_bridge`; Dart native `a marked pantry item survives reopening the same file`; widget `the pantry screen lists identities and marks one`, `unmarking sends marked false and the row returns to unmarked`; on-device below. **AC-2 PASS, argued rather than asserted** — the *recipe* arm is proved by `a pantry failure does not block the recipe detail` (pantry in `AsyncError`: lines and instructions render, **no** `describeFailure(…, subject: 'Pantry')` text appears anywhere, `takeException()` null) plus the empty/partial cases `an_empty_pantry_lists_every_identity_as_unmarked`, `an_empty_household_lists_the_catalog_as_unmarked`, widget `an empty pantry is honest about what it does not know` and `the pantry screen renders a load failure and no list`. The *planning* and *shopping* arms are proved by the fact that **no planning or shopping surface consumes pantry in this card** — `MVP-015` and `MVP-016` are `Draft`, `grep` finds no pantry reference outside `lib/features/pantry`, `recipe_detail_screen.dart` and the generated bindings — together with the pre-existing planning/shopping suites passing unchanged. This is stated so an independent verifier can check the claim rather than accept a bare `PASS` (README:68). **AC-3 PASS, in two halves.** *Household scoping:* `pantry_reads_and_writes_are_household_scoped` (adversarial, second-household fixture — h1's mark leaves h2 unmarked, and h2 unmarking it does **not** clear h1's row), `a_custom_ingredient_of_another_household_cannot_be_marked` (typed `CustomIngredientHouseholdMismatch`, zero rows), `list_pantry_entries_covers_the_catalog_and_only_this_households_custom_ingredients`, `marking_an_absent_ingredient_is_rejected_and_writes_nothing`, `marking_for_an_absent_household_is_rejected`; bridge `commands_are_scoped_to_the_named_household`, `a_custom_ingredient_of_another_household_is_rejected`, `an_unknown_ingredient_is_rejected`. *Accessible state:* `the pantry screen meets the accessibility guidelines` runs `androidTapTargetGuideline`, `labeledTapTargetGuideline` and `textContrastGuideline` in light **and** dark on Pixel-5 geometry, and asserts `find.bySemanticsLabel(pantryRowLabel(name, marked))` as an **exact** string for every row (a `tester.ensureSemantics()` handle is required and is disposed inline, since the framework checks for live handles before tear-downs run). The claim is worded precisely: **the accessible name states the meaning before the platform toggle state** — "chickpeas, not marked" — **not** that "off" is never announced. "on"/"off" is synthesised by the screen reader from `SemanticsFlag.hasToggledState`, which no label can suppress, and its absence is unprovable in a widget test, so no such assertion was written. Text scale 2.0 is covered by the existing `destination screens survive text scale 2.0`, which loops over `labels` (including `'Pantry'`) and from this card exercises the live screen; its stale "placeholder bodies" comment was corrected. **Correction to the plan's AC-3 mechanism, found by the test rather than by reading:** the plan specified `Semantics(label:)` *around* `SwitchListTile`. That fails `labeledTapTargetGuideline` — `SwitchListTile` returns a `MergeSemantics` whose merged node is the tappable one, so an outside wrapper becomes a separate parent and leaves the tappable node with **no name at all**. The label was moved *inside* the tile (`title: Semantics(label: …, child: ExcludeSemantics(child: Text(name)))`), which both satisfies the guideline and makes the merged name exactly `pantryRowLabel`. The custom-ingredient `subtitle` is also `ExcludeSemantics`-wrapped so the merged name stays exact; the cost, stated rather than hidden, is that the visual "Your ingredient" distinction is not in the announcement — the name and mark state are, which is what AC-3 requires. **Search includes aliases** (`the pantry search finds an identity by its alias`): `ingredient_alias` ships 27 aliases of which 17 are not substrings of their canonical name (`garbanzo beans`→chickpeas, `prawns`→shrimp, `carrots`→carrot), so a name-only filter would return nothing for the word the user knows, against a scope line that says "browse/**search**". Aliases are attached by **one** query folded into a `HashMap` keyed on `kind = 'catalog'` — not the bare id, so a custom ingredient sharing an id string cannot inherit catalog aliases — and `catalog_entries_carry_their_aliases_and_custom_entries_carry_none` pins it. **`ORDER BY name COLLATE NOCASE, kind, id` is load-bearing** and pinned by `pantry_entries_are_ordered_case_insensitively`: BINARY collation would have sorted every capitalised custom name (`Bacon`) ahead of the entire lowercase catalog (`black pepper`). `list_custom_ingredients` has the same latent issue, unexposed only because all its names are user-typed; it was **not** changed here. **Behaviour-preserving refactors, each pinned by existing tests only:** `check_ingredient_ref(conn, household, ingredient)` extracted from `check_line_refs` and shared with `set_pantry_mark`, so a foreign or absent identity is rejected identically whichever way it arrives (note the Custom-not-found arm returns `NoSuchIngredient`, not `NoSuchCustomIngredient`); `load_aliases` extracted from `load_ingredient` and reused by the mark read-back; `ref_to_domain`/`ref_from_domain` extracted `pub(crate)` in `recipe.rs` with the `Option` handled at each call site, and reused by `pantry.rs` rather than duplicating the match. FRB cross-module DTO reuse is precedent, not risk (`recipe.rs` already reuses `RestrictionDto`; `planned_meals.rs` reuses `MealSlotDto`), and `pub(crate)` functions are not exported. The storage functions are called **fully qualified** from `pantry.rs` to avoid an `E0255` collision with the module's own `set_pantry_mark`. `INSERT … ON CONFLICT DO NOTHING` is **untargeted**: a targeted `ON CONFLICT(household_id, ingredient_id)` does not prepare against a partial index unless the index's `WHERE` clause is repeated, and untargeted still propagates CHECK violations. No `require_household` on the read: an absent household reads as the catalog with nothing marked, the same "reads as empty" contract `load_restrictions` states. **Deliberate exclusions:** no store-category grouping (`MVP-015`'s derivation contract), no `marked_on` column (PRD §12's append-orientation is scoped to planner/audit records), no pantry seeding or onboarding prompt (card non-goal). **Gates, all rc 0**, each redirected to its own file with `$?` read separately and none piped through a filter: `dart format --output=none --set-exit-if-changed .`, `flutter analyze` (0 issues), `cargo fmt --all --check`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo test --workspace` **369** (baseline **348** measured at Step 1: food-domain 112, household-core 2, kimatta-storage 170→186, bridge 64→69 — +21, exactly the tests added), `cargo build --release` + `flutter test` **203** (baseline **192**; +11 = 8 pantry widget, 2 recipe-detail widget, 1 bridge-native), `flutter build apk --debug`; `cargo tree --workspace -i rusqlite` one version (0.40.2). **Invariants:** 6 — no quantity, expiry or `present` column exists; 16 — no unused third state; 17 — `grep -rn "sqlite\|sqflite" lib/` empty, only Rust writes `pantry_item`; 19 — no surface claims stock completeness, pinned by `pantryDisclaimer` ("Anything unmarked is simply unknown — it is not a record that you are out.") and by the `marked` field name; 21 — the bridge gains exactly **two** coarse service-level commands, and `git diff --stat` shows the five generated files changed only by `flutter_rust_bridge_codegen generate` plus the new generated `lib/src/rust/api/pantry.dart`, none hand-edited. **On-device evidence, `emulator-5554`, AVD `pre001_avd`, debug APK, over the D-033 bridge; nothing installed or activated beyond the debug APK (invariant 15, card External actions `None`):** installed and launched, opened **Pantry**, the catalog rendered; `uiautomator` before the tap read `content-desc="black beans, not marked" checkable="true" checked="false"`; after the tap, `content-desc="black beans, marked as in your pantry" checkable="true" checked="true"`. The `checkable`/`checked` attributes **are** the platform toggle state and are expected — recorded rather than implied absent. `am force-stop` + relaunch → Pantry re-read `"black beans, marked as in your pantry" checked="true"` (AC-1 persistence on device). `run-as dev.mealmate.temp sqlite3 …/files/kimatta.db`: `PRAGMA user_version` **8**, `SELECT * FROM pantry_item` exactly **one** row `609bcca7-…|ing-black-beans|` scoped to the bootstrapped household (`SELECT id FROM household` matches), `PRAGMA integrity_check` **ok**; `adb logcat -s AndroidRuntime` **0** `FATAL EXCEPTION`. **One planned on-device reading is `NOT VERIFIED` and does not block:** the in-recipe control could not be exercised on device because **no recipe on the device carries a line resolving to a catalog identity** — `MVP-011`'s starter recipes are all pending cook review so none installed, and the recipe form can only mint *custom* ingredients (`IngredientRefDto` doc: no bridge command creates catalog rows). The device's only recipe has no ingredient lines at all. This is a fixture limitation of the current device state, not a gap in this card's code; the arm is covered by `the recipe detail toggles pantry for a resolved line only`, which uses a purpose-built resolved-line fixture (every `okRecipe` line is unresolved) and asserts exactly one control exists and the tap reaches the seam with that ref. It is discharged whenever a starter recipe is installed (`MVP-011` AC-3's cook log) or `MVP-013`/`MVP-015` puts catalog-resolved lines on device. **Recipe-context scope:** the affordance is on `recipe_detail_screen.dart`'s ingredient lines only — not `recipe_form_screen.dart` (an unsaved line has no committed identity) and not `recipe_list_screen.dart`. It is deliberately **not** routed through the screen's `_run` helper, which navigates to `/recipes` on *success* and gates Delete/Restore with a screen-wide `_busy`; marking keeps its own per-row busy set. **Honest limits:** the Rust tests for steps 4–5 reference types that do not exist before their own step, so their fail-first is a compile failure rather than an observed assertion failure (the same limit `MVP-009`/`MVP-011`/`MVP-012` recorded); the five step-2 schema tests **did** fail first for real ("no such table: pantry_item", and `6 != 7`-style `8` mismatch); step 3's refactor is expected-to-pass and its three named pins passed unchanged; the pantry **provider** has no standalone test — the project has no provider test harness and adding one for a single notifier would not match its neighbours, so it is exercised through the screen; the step-7 screen was written before its widget tests (the step-8 recipe control was not, and failed first as intended); and one plan instruction was not followed — `MIGRATIONS.to_version(&mut conn, 7)` inside `the_component_recipe_foreign_key_is_indexed` was **left at 7**, because that test's whole claim is that *migration 7* ships those indexes and bumping it would silently weaken it to "the latest schema has them"; the tree is green with it at 7. **Next implementation task: none.** `MVP-014` sits at `Verify`; under `docs/task/README.md:31` the owner-delegated approval is the union of the orchestrator's review, fix loop, an independent verifier `PASS` and the merge to `integration`, none of which this session performs, and the orchestrator then writes the `Done` row and promotes the next card itself. The next card is `MVP-013` (`SEQUENCE.txt` step 26), named here only as what the orchestrator promotes at `MVP-014`'s `Done`: promoting it now would break its own planning-gate check 3 against this `Verify` row — the deadlock `MVP-011`'s row and commit `72a7301` both record refusing to create. Not committed or pushed. |
| 2026-08-29 | MVP-014 | Done | Orchestrated approval under `# orchestrate: auto-approve`: review `redteam-impl-handoff-orch-25-2026-08-29T1633-ef03.md`; 1 fix round(s); independent verifier `PASS` (session `2880047f-efeb-4161-8a4f-b4d194b6c93f`); merged into `integration` as `df7133962d70c5512556bd007955b2e34734227a`. Owner ratifies at the `integration → master` merge. |
| 2026-08-29 | MVP-013 | Verify | Planner screen over `planning_cycle_window` (new Rust query: the window containing today ± n cycles, calendar arithmetic in `food-domain`; 6 domain + 1 bridge + 1 native pin). AC-1 widget half PASS — 19 widget tests (render per date×slot, add/move/scale/lock/remove, picker `open` rule, freeform note, archived/unknown titles, error+retry, failed-save snackbar, cycle paging); emulator half NOT VERIFIED (run budget exhausted before `tools/emulator.sh`). AC-2 PASS — lock label via `find.bySemanticsLabel`, conflict subtitle + tap-through, a11y guidelines pass with a meal. AC-3 PASS for widget states (empty/loading/error/retry, text scale 2.0); manual matrix NOT VERIFIED. Suites: cargo fmt/clippy/test rc 0 (food-domain 118, bridge 70); `flutter analyze` clean; `dart format --set-exit-if-changed` clean; `flutter test` 245 pass, 0 fail (the MVP-003 pin `changing initialLocation on a live App element is rejected` is now provoked from `/shopping`: with the planner mounted, the deliberately half-updated tree also raised a `_FocusInheritedScope` teardown assertion, and a test reporting two exceptions fails regardless of `takeException`; the pin's own assertion is `App`'s and branch-independent); `flutter build apk --debug` rc 0. Open note (not a contract): offset 0 = the *whole* window containing today; MVP-023/024 decide whether they want only its remaining days. |
| 2026-08-29 | MVP-013 | Done | Orchestrated approval under `# orchestrate: auto-approve`: review `redteam-impl-handoff-orch-27-2026-08-29T1804-63bd.md`, `redteam-impl-handoff-orch-27-2026-08-29T1900-494b.md`; 2 fix round(s); independent verifier `PASS` (session `38c1a8c7-69d7-4820-8e69-0d1c49f80dec`); merged into `integration` as `bf9879bcb4a98fe3e5429fa2cda06037b21c0598`. Owner ratifies at the `integration → master` merge. |
| 2026-08-29 | MVP-015 | Verify | **Option A: pure derivation, no schema change, no package added (D-031).** `food_domain::shopping::derive_shopping_list(&ShoppingInput) -> ShoppingList` is pure and clock-free; `kimatta_storage::load_shopping_input`/`load_shopping_list` read the snapshot in one transaction (archived recipes included, one `load_recipe` per distinct id; the pantry marks come from `list_marked_pantry_refs`, one narrow statement per identity kind whose `WHERE` repeats the covering partial index's own predicate, **not** from the `list_pantry_entries` browse query, which materialises every catalog row and the whole alias table to answer the same question); the bridge exposes one read-only command `derive_shopping_list(household_id, from_date, to_date)` in `rust/src/api/shopping.rs` with `Contribution.scale` crossing as `ScaleDto`. No `KimattaError` variant added. **Supported conversion families (exact integer factors to the family base; cross-system pairs deliberately absent):** VolumeUs tsp=1/tbsp=3/fl_oz=6/cup=48; VolumeMetric ml=1/l=1000; MassMetric g=1/kg=1000; MassUs oz=1/lb=16; Count piece=1. A merged line renders in the largest contributing unit; same-kind contributions add directly without a base conversion. **Merge contract:** contributions merge iff same `IngredientRef` (unresolved never merges), same `optional`, same unit-class (exact family / `Unit::None` / byte-identical `Other` text), and both known or both `Unknown`. **Key format for MVP-016:** `m:{catalog|custom}:{id}:{volume_us|volume_metric|mass_metric|mass_us|count|none|other=<text>}:{opt|req}:{known|unknown}` for merged lines, `s:{planned_meal_id}:{component_position}:{line_position}` for per-contribution lines; keys embed no name or category. Every caller-supplied segment (`{id}`, `<text>`, `{planned_meal_id}`) is backslash-escaped — `\` as `\\`, `:` as `\:` — so a key splits into its fields on its *unescaped* colons. Without the escape the `m:` key was ambiguous: `id_newtype!` accepts `:` verbatim ("Accepts the id verbatim; rejects empty/whitespace, never trims") and `unit_to_domain` passes `Other` text through unvalidated, so a custom id `x:other=b` on a `Unit::Other("c")` line and a custom id `x` on a `Unit::Other("b:other=c")` line both rendered `m:custom:x:other=b:other=c:req:known`, and a MVP-016 edit keyed on that string would have hit both lines silently. Pinned by `line_keys_are_unique_when_ids_and_unit_text_contain_colons` (the collision above, set of keys sized against the line count) and `line_keys_stay_unique_when_ids_and_unit_text_contain_backslashes` (adds a pair that collides under a colon-*only* escape, so `\` handling is pinned too, plus the exact published encoding of one key); `other_unit_text_containing_a_colon_still_merges_only_with_identical_text` pins that escaping — being injective — left the byte-identical-`Other`-text merge discriminator where it was. **Omitted/restore contract:** a marked identity's line is returned with `status = OmittedPantryMarked`, quantity and provenance intact; restore is MVP-016's persisted decision. `separate_reason` on `m:` lines is informational, computed per `(identity, optional)`: `UnknownQuantity` when an unknown-quantity group sits beside a known one — **this clause takes precedence**, because a missing amount is a property of *that group* while a unit split is a property of the identity's whole set — otherwise `UnitNotCombinable` when >1 unit-class group exists **among the groups sharing this group's known-ness**. Counting classes per known-ness is what keeps the label honest in both directions: an unknown group is never told its units did not combine, and a known group is never told so by an unknown sibling it could not have combined with anyway. Overflow: any checked add/mul returning `None` makes the whole group fall back to `s:` lines carrying `ArithmeticOverflow` and each contribution's scaled-or-original quantity — never a partial sum; an **unresolved** line whose own scale overflowed carries `ArithmeticOverflow` rather than `Unresolved` for the same reason, since it too shows the quantity as written and a 1000×-wrong number must not be presented as a correct one. A component whose recipe is missing from the snapshot becomes an `s:` line named by the recipe id with `Unresolved`. An inverted range derives an empty list (`list_planned_meals` precedent). `Rational::checked_add`/`checked_mul` added additively in `recipe.rs` (private fields untouched). **AC-1 PASS** — `a_scaled_component_multiplies_every_known_quantity`, `an_unscaled_component_is_one_times`; storage `an_inclusive_date_range_selects_exactly_the_planned_occurrences` (1 cup ×2 meals → 2 cup); bridge `a_planned_cycle_derives_a_grouped_list_through_the_bridge` (1 + 3/2 → 5/2 cup). **AC-2 PASS** — `a_marked_identity_is_omitted_with_its_quantity_and_provenance_intact`, `an_unmarked_identity_is_needed` (empty pantry → all `Needed`), `an_unresolved_line_cannot_be_omitted`, `a_marked_identity_on_an_overflow_fallback_line_is_still_omitted`; storage `a_marked_pantry_identity_arrives_in_the_snapshot`. **AC-3 PASS** — `every_same_family_pair_merges_and_every_cross_family_pair_stays_separate` loops all 121 ordered `UnitKind` pairs against a hard-coded factor table (29 same-family merge with value `(f_a+f_b)/max`, 92 cross-family stay apart flagged `UnitNotCombinable`), `merged_lines_render_in_the_largest_contributing_unit` (1 cup + 2 tbsp + 1 tsp = 55/48 cup), `same_kind_contributions_add_without_a_base_conversion`, `none_merges_only_with_none_and_other_only_with_identical_text`, `a_line_with_other_unit_text_differing_only_by_case_stays_separate` (adversarial), `unknown_quantities_merge_into_one_unknown_line_and_never_with_known_ones`, `optional_and_required_lines_of_one_ingredient_stay_apart`, `unresolved_lines_never_merge_and_keep_original_text`, `an_overflowing_group_falls_back_to_separate_lines_regardless_of_order` (scale 1000/1 × u32::MAX cups), `an_overflowing_sum_falls_back_even_when_every_scale_fits`; `rational_add_reports_overflow_instead_of_wrapping` includes the `u32::MAX/(u32::MAX-1)` self-sum whose two `u64` products fit but whose sum does not. **AC-4 PASS** — `every_permutation_of_meals_recipes_and_pantry_yields_the_same_list` (24 meal permutations × reversed recipes/identities/pantry, whole-`ShoppingList` equality), `every_contribution_is_accounted_for` (`contribution_count` == Σ lines == 18, contributions ordered `(date, slot, meal, component, line)`), `groups_are_in_category_order_with_uncategorised_last`, `a_merged_line_is_named_by_its_identity_not_the_first_spelling`, `a_resolved_ref_with_no_identity_row_still_merges_and_is_uncategorised`, `non_recipe_components_are_counted_and_contribute_nothing`, `a_component_whose_recipe_is_missing_from_the_snapshot_is_reported_not_dropped`, `derived_quantities_are_never_zero_or_negative`, `rational_add_is_commutative_on_a_boundary_matrix`; storage `shopping_snapshot_is_household_scoped` (adversarial: h2's meals, marks and custom ingredient never reach h1), `store_categories_come_from_the_catalog_and_custom_rows`, `shopping_snapshot_reads_an_archived_recipes_lines`, `shopping_snapshot_for_an_absent_household_is_rejected`; bridge `commands_are_scoped_to_the_named_household`, `dto_mapping_round_trips_every_unit_and_quantity_shape` (scaled `ScaleDto{2,1}`, `Range`, `Other`, `Unknown`/`None`, pantry-omitted, unresolved `s:` key), `a_non_civil_date_is_a_typed_planning_error`, `an_inverted_range_is_empty_not_an_error`; the bridge test re-reads the same fixture and asserts DTO equality. **Performance (measured, no timing assert, PRD §18):** `a_full_31_day_three_slot_cycle_derives_in_bounded_work` — 93 meals × 3 recipe components × 30 lines = 8,370 contributions derives in **9.0 ms** (debug build, WSL2). The figure is domain-only and excludes SQLite I/O; the one part of the surrounding read whose cost would have scaled with *catalog* size rather than plan size — the pantry marks — is now an index-matched statement, so the exclusion no longer hides a catalog-sized read. **Counts vs Step 0 baseline** (measured this session: cargo **377** = food-domain 118, household-core 2, storage 187, bridge 70; `flutter test` **256** — the plan's "351-ish"/245 were stale): food-domain 118→156 (+38), storage 187→195 (+8), bridge 70→75 (+5) = **428** cargo tests (+51 = 156 + household-core 2 + 195 + 75; the earlier **411** in this row omitted household-core from the sum its own baseline included, and the **425**/153 that replaced it predated the three line-key tests the fix round added), and `flutter test` **256→257** (+1, the bridge-native shopping pin). Both totals are read from `cargo test --workspace` and `flutter test` output in the fix round, not arithmetic on the deltas. **Gates:** `cargo test` per crate rc 0 (each crate run as its own invocation); `cargo fmt --all` applied; `cargo clippy --workspace --all-targets -- -D warnings` rc 0; `flutter_rust_bridge_codegen generate` rc 0 (creates `lib/src/rust/api/shopping.dart`, regenerates `frb_generated.*`). **Gates owed at `Verify` and run in the fix round, all rc 0**, each a single invocation with its exit status read from the tool result and none piped through a filter: `dart format --output=none --set-exit-if-changed .`, `flutter analyze` (No issues found), `cargo fmt --all --check`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo build --release` then `flutter test` **257** against the freshly rebuilt `.so` (the `.so` on disk had predated `rust/src/api/shopping.rs`, so a `flutter test` run before the rebuild would have passed against native code lacking the command), `flutter build apk --debug`. The `bridge_native_test.dart` pin `a derived shopping list groups a resolved line and omits a marked one` **was added** and is the only Dart execution of the generated shopping decoders anywhere in the tree: it asserts all four decode branches this card newly introduced to the bridge surface — `separate_reason` **null** on a merged line and `Some(Unresolved)` on an `s:` line, and `Contribution.scale` both **null** and `ScaleDto(2,1)` inside the nested contribution list — plus the pantry omission keeping its quantity and `contribution_count` 6 across four lines. Invariants: 17 — `grep -rn "sqlite\|sqflite" lib/` empty; 21 — `git status --short` carries the four regenerated `frb_generated.*` files plus the generated `lib/src/rust/api/shopping.dart` and no hand-edit to any of them. **Still NOT RUN:** on-device evidence — no UI consumes the command until MVP-016, and `flutter build apk --release` is not in this card's owed set. **Deviations, stated:** Step 4's storage function was written before its tests (the tests were then written and observed passing, not failing first); `save_recipe_in` and `planned_meals::save_in` became `pub(crate)` so the bridge test can seed through the real write path; `separate_reason` is scoped per `(identity, optional)` rather than per identity alone, so an optional/required split is not mislabelled `UnitNotCombinable`. Fail-first for steps 2, 3 and 5 was a compile failure (observed), as prior cards recorded. On-device: not run — no UI consumes the command yet. Not committed or pushed. |
| 2026-08-29 | MVP-015 | Done | Orchestrated approval under `# orchestrate: auto-approve`: review `redteam-impl-handoff-orch-29-2026-08-29T2047-558f.md`, `redteam-impl-handoff-orch-29-2026-08-29T2126-026f.md`; 2 fix round(s); independent verifier `PASS` (session `2f4355a7-4bbf-495b-8e7f-0dfde27ff3e7`); merged into `integration` as `4e8552bb6ec8598c63d69f1b0d0ab11b8927f0aa`. Owner ratifies at the `integration → master` merge. |
| 2026-08-29 | MVP-016 | Verify | **Completion round (plan steps 8–14) over the Rust core recorded in the row below.** Flutter surface: `lib/features/shopping/shopping_copy.dart` (pure copy, `describeQuantity`, `describeCheckedAgainst` — renders a stored token like the live line, verbatim if unparseable — `changedCopy`, `resetBody` naming both counts, `sectionFor` per decision 6), `shopping_provider.dart` (`ShoppingNotifier`, autoDispose family by cycle offset; `build` *watches* `planningCycleProvider.future`, `plannerProvider(arg).future` and `pantryProvider.future` so a cycle save, meal save or pantry toggle re-runs it — decision 8; seven `@protected` seams; `refresh()` reads the shared anchor and never commits one; `setLineState` republishes by key and drops a cleared row; `saveManualItem` republishes in name/id order; `addCheckedToPantry`/`undoPantryMarks` return the changed refs and `ref.invalidate(pantryProvider)`; `reset()` re-fetches), `shopping_screen.dart` (cycle arrows, overflow menu with **Add checked to pantry** enabled iff the decision-7 eligible set is non-empty and **Start over** behind a confirm naming both counts; per-category **To buy** rows as `Row(Expanded(CheckboxListTile(title: Semantics(label: lineLabel…))), IconButton('Why is this here?'))` — the explain button outside the tile so it is its own node; a changed line renders **unchecked** with `changedCopy`; explain sheet with contributions, reason, omitted copy, **Remove from list**, and **Back to pantry** only on `omitted && restored`; **Your items** with add/edit/delete dialog owned by a `StatefulWidget` (name required); **Already have**/**Removed** `ExpansionTile`s with **Add anyway**/**Put back**; `AsyncLoading` *with a value* renders the previous view so a dependency-driven reload never blanks the list; `describeFailure(e, subject: 'Shopping')`), `router.dart` `/shopping` → `ShoppingScreen`. **Widget tests (13, `test/app_test.dart`, `_FakeShoppingNotifier` overriding only the seams, unconditional `shoppingProvider` override):** AC-1 `the shopping screen groups lines and explains one` (category headings, `Other`, Already have, both contributions in the sheet); AC-2 `checking a line sends its state and renders what was stored` (key + flags sent, `lineLabel('flour', true)` semantics, uncheck clears), `a changed line renders unchecked with the changed copy` (and is not pantry-eligible), `removing a line moves it to Removed and Put back restores it`, `an omitted line can be added anyway and sent back to pantry`, `manual items can be added, checked, edited and deleted` (blank name refused in-dialog, blank id sent so Rust mints, case-insensitive order); AC-3 `add checked to pantry sends only the eligible refs and Undo sends the returned set` (unresolved checked line excluded; Pantry tab shows the mark; Undo sends exactly the returned set; the shopping build re-ran after the invalidation), `Start over names the counts and clears only after confirm` (Cancel writes nothing; confirm sends the window's dates); `the shopping screen renders a load failure in prose` (`Shopping unavailable: boom`), `an empty derivation shows the empty copy`, `a pantry toggle re-reads shopping and a reload keeps the list` (fetch count 1→2; previous list on screen with no spinner while the re-read is pending), `a planner save re-reads shopping`; AC-4 `the shopping screen meets the accessibility guidelines` (three guidelines, light+dark, exact `lineLabel` per row, two `Why is this here?` nodes). `test/shopping_copy_test.dart` (7: safety-claim regex, every `describeQuantity` shape, token rendering incl. escaped `Other` text and verbatim fallback, `explainContribution` with/without scale, `resetBody`, hard-coded `sectionFor` table). `bridge_native_test.dart` `a shopping view survives reopening the same file` (AC-2 restart at the real bridge; AC-3 `setPantryMarks` flips the line to omitted and the returned refs flip it back; reset keeps the unchecked item; deleting twice is `Storage`; blank name is `KimattaError_Shopping`). **Fail-first:** the widget and copy tests were compile-fail-first (new modules); the native pin is expected-to-pass and labelled. **Fixes found by the tests, not by reading:** the item dialog first disposed its controllers on the route future, which completes before the exit animation — a "used after being disposed" assertion that corrupted the tree for every later test; the dialog now owns them as a `StatefulWidget`. The MVP-003 pin `changing initialLocation on a live App element is rejected` reported the predicted second focus-scope assertion (R2-7) — and did so from `/settings` and `/welcome` too, so no branch is inert any more; it now unmounts the tree in its own frame and takes that teardown assertion separately, keeping its own claim intact. The native pin compared `ShoppingListDto`s with `==`, which the generated code implements by `List` identity; it compares fields. **KNOWN_ISSUES-low `orch/31`:** the dead `pub(crate)` widening on `quantity_to_domain`/`unit_to_domain` reverted (RESOLVED); the other three LOW items stay OPEN as recorded. **Gates, all rc 0, each a single invocation with its exit status read from the tool result:** `dart format --output=none --set-exit-if-changed .`; `flutter analyze` (No issues found); `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings`; `cargo test --workspace` **464** (food-domain 159, household-core 2, storage 216, bridge 87; Step 0 baseline 428); `cargo build --release` then `flutter test` **278** (baseline 257: +7 copy, +13 widget, +1 native); `flutter build apk --debug`. Invariants: 17 — `grep -rn "sqlite\|sqflite" lib/` empty; 21 — generated files changed only by codegen; non-goals untouched (no routing, prices, scanning). **On-device: NOT RUN (optional)** — not in the card's evidence plan. AC-1 PASS, AC-2 PASS, AC-3 PASS, AC-4 PASS at both layers (fresh-context review = the orchestrator's red-team). **Next implementation task: none** — the orchestrator promotes `MVP-023` at `Done`. Not committed or pushed. |
| 2026-08-29 | MVP-016 | In Progress | **Partial: Rust core complete (plan steps 0–7), Flutter surface not started (steps 8–13); the session's USD budget ran out before the screen.** Status stayed `In Progress`, not `Verify` — AC-1's grouped UI, AC-2's check-off/edit UI and AC-4's semantics check had no widget half yet (completed in the row above). **Decisions 1–10 recorded in the card and implemented in Rust:** live derivation + per-line overlay (Option A); merge by default, overwrite only via explicit `reset_shopping_list`; `checked_against` = `quantity_token(quantity, unit)` and the view reports `changed = true` instead of keeping a stale check; line states window-keyed (orphaned by a cycle edit, pinned by `a_changed_window_reads_no_states`); manual items household-scoped; `checked` a plain boolean; all-false rows deleted; `set_pantry_marks` returns only changed refs (R2-4). **Schema v8 → v9**, one migration: `shopping_line_state` (PK `(household, from, to, line_key)`, CHECKs: non-blank key, some flag set, `checked ⇔ checked_against`) and `shopping_manual_item` (trimmed non-blank name, household index); nine `== 9` pins + eight `…_to_v9…` names + `bridge_native_test.dart` `schema v9` bumped; `the_component_recipe_foreign_key_is_indexed` left at 7 (MVP-014 precedent). Domain: `ShoppingManualItemId`, `ShoppingError { BlankName, BlankKey, UnknownLineKey, CheckWithoutQuantity }`, `quantity_token` (`quantity_token_distinguishes_every_shape`, `quantity_token_is_stable_for_equal_inputs`, `a_blank_manual_item_id_is_rejected`). Storage: `list/set_shopping_line_states`, `list/save/delete_shopping_manual_item`, `reset_shopping_list`, `set_pantry_marks` (+ `set_pantry_mark_in` extracted, behaviour-preserving, pantry suite unchanged), `StorageError::{NoSuchShoppingItem, Shopping}`. Bridge: `KimattaError::Shopping`; `load_shopping_view`, `set_shopping_line_state` (Dart sends key + flags only; Rust re-derives the list, takes the token from the line that key names, and rejects an unknown key as `UnknownLineKey` — a clear, all flags off, skips the derivation so orphans stay clearable one line at a time), `save/delete_shopping_manual_item`, `reset_shopping_list`, `pantry::set_pantry_marks`; codegen + `build_runner` run, generated files changed only by codegen; `describeFailure` gained the `KimattaError_Shopping` arm. **Tests (fail-first observed):** 12 schema tests failed `8 != 9` / `no such table` before the migration; new-function tests were compile-fail-first (prior cards' recorded limit). Storage: `an_existing_v8_database_migrates_to_v9_without_losing_data`, `a_shopping_line_state_row_with_no_flag_set_is_rejected`, `a_checked_line_state_without_its_quantity_is_rejected`, `a_check_without_its_quantity_is_a_typed_error_not_a_check_violation`, `a_blank_manual_item_name_is_rejected`, `deleting_a_household_cascades_to_its_shopping_rows`, `a_line_state_persists_and_reads_back`, `clearing_every_flag_removes_the_row`, `line_state_is_scoped_to_household_and_window`, `a_changed_window_reads_no_states`, `line_state_for_an_absent_household_is_rejected`, `line_state_persists_across_reopen` (AC-2 restart, storage half), `a_manual_item_saves_and_lists`, `saving_an_existing_id_replaces_it`, `a_manual_item_of_another_household_cannot_be_replaced_or_deleted`, `manual_items_are_ordered_case_insensitively`, `reset_clears_this_windows_states_and_only_checked_items`, `a_blank_manual_name_is_rejected_before_any_write`, `bulk_marks_land_in_one_transaction_or_not_at_all`, `bulk_marking_returns_only_the_refs_it_changed`, `bulk_marking_is_idempotent_and_reversible`. Bridge: `a_view_carries_the_derivation_and_both_overlays` (AC-1 data half), `a_checked_line_whose_quantity_changed_is_reported_changed`, `an_orphaned_state_is_not_in_the_view`, `an_orphaned_state_can_still_be_cleared_by_key`, `a_state_for_a_key_not_in_the_derivation_is_a_typed_error`, `a_check_is_bound_to_the_line_its_key_names`, `overlay_commands_are_scoped_to_the_named_household` (AC-4 Rust half), `a_blank_manual_name_is_a_typed_shopping_error`, `a_blank_line_key_is_a_typed_shopping_error`, `bulk_pantry_marks_flip_lines_to_omitted_and_back` (AC-3 bridge half), `reset_leaves_the_derivation_and_unchecked_items`. **Gates run, all rc 0:** `cargo fmt --all` applied; `cargo clippy --workspace --all-targets -- -D warnings`; `cargo test --workspace` **464** (Step 0 baseline **428**: food-domain 156→159, household-core 2, storage 195→216, bridge 75→87; +36, the last +5 from the redteam remediation pass below); `flutter analyze` (No issues found); `dart format --output=none --set-exit-if-changed .`; `cargo build --release`; `flutter test` **257** against the rebuilt `.so` (rc 0, baseline **257**; no Dart tests added yet, the v9 native pin passes). **Redteam remediation (same session, 5/5 findings fixed, 0 deferred):** the three MEDIUMs and both LOWs from `redteam-impl-handoff-orch-31-2026-08-29T2237-2591.md` are closed. `set_shopping_line_state` lost its `quantity`/`unit` parameters (F1) — the coupling the Flutter half would have had to maintain by hand is gone before that half is written; `ShoppingViewDto` gained `orphaned_line_state_count: u32` so a dropped edit is reported rather than silently lost (F2, the card's stop condition); AC-1's evidence row regained its widget half (F3); storage rejects a check with no token as a typed error (F4); the card now documents the unbounded per-window retention (F5, documentation branch — no purge, deleting user data needs a retention policy no PRD sets). Both KI-low entries filed by that review are `RESOLVED`. A follow-up `/redteam-code` pass over `rust/src/api/shopping.rs` raised two MEDIUMs against the remediation itself, both fixed: the clear-path comment overstated what Dart can reach (orphan keys are not surfaced, so `reset_shopping_list` is still the only *reachable* clear — a UI-half decision, recorded not coded), and `hidden`/`restored` had no live-line test, now pinned by `hidden_and_restored_come_back_on_the_flags_they_were_written_on`. That pass filed five LOWs to `KNOWN_ISSUES-low.md`, none regressions from this work. Codegen re-run; generated files changed only by codegen and only for these two shape changes. **Not done, for the next session (plan steps 8–13 + 14's remaining gates):** `lib/features/shopping/shopping_copy.dart` + `test/shopping_copy_test.dart`; `shopping_provider.dart` (`ShoppingNotifier` watching `plannerProvider(arg).future` and `pantryProvider.future`, seams, no anchor commit); `shopping_screen.dart` + `router.dart` swap; `_FakeShoppingNotifier` + unconditional `shoppingProvider.overrideWith` in `test/app_test.dart` and the widget tests the plan lists; `bridge_native_test.dart` `a shopping view survives reopening the same file`; accessibility test; `flutter build apk --debug`; on-device pass (optional). AC-1/AC-2/AC-3/AC-4: **NOT VERIFIED** at the UI layer; Rust halves PASS as named above. No `.so`-stale risk at handoff: release build ran after the last Rust edit. **Next implementation task: none** — `MVP-016` occupies the lane at `In Progress`. Not committed or pushed. |
| 2026-08-30 | MVP-016 | Done | Orchestrated approval under `# orchestrate: auto-approve`: review `redteam-impl-handoff-orch-31-2026-08-29T2237-2591.md`, `redteam-impl-handoff-orch-31-2026-08-29T2331-d2ca.md`; 1 fix round(s); independent verifier `PASS` (session `2680c4a1-5924-4745-a0d4-7a3a063e614e`); merged into `integration` as `1d39e8e4c80635351433bba10a13eab4351151dc`. Owner ratifies at the `integration → master` merge. |
| 2026-09-02 | MVP-023 | Verify | Plan `read-docs-prd-v3-md-docs-household-contr-eager-spark.md` rev 2, executed 2026-09-02. **Delivered:** `household-core/src/kernel.rs` (EvidenceSource/Confidence/Band/Urgency/Reversibility/RequiredAuthority/OutcomeStatus, ReasonCode no-whitespace, Policy tab/newline-safe parameters, OutcomeAssessment/ActionProposal/AttentionRequest/LedgerEntry, tiny `HouseholdController` trait; deps `thiserror` only per `cargo tree`); `food-domain/src/planner/` (snapshot + hand-written canonical text + FNV-1a 64 hash, seven §9.3 sources incl. generate-and-reject `LOCK_CONFLICT`, Tier-0 `RESTRICTION_CONFLICT:rules_v1`/`HARD_VETO`/`PREP_WINDOW_IMPOSSIBLE`, reason-coded `Score([i64;6])` lexicographic tiers with §9.10 floor-then-`Σ−2×sacrifice`, canonical-order beam with text tie-break and `states_scored` trace, §2.2 coverage + §10 sufficiency via one `ISSUE_TEXT` table, §11 attention, `FoodController` assess/propose, pure `cover_cycle`); schema **v10** `policy` + append-only `controller_ledger` (BEFORE UPDATE/DELETE triggers, household FK no cascade), `save/delete_planned_meal_in` extraction (existing suites untouched), `save_policy`/`list_policies`/`append_ledger_entry(_in)`/`list_ledger_entries`/`apply_plan_and_record` (one Immediate tx, unchanged slots keep ids, locked conflict rolls back whole)/`load_planning_snapshot` (one read tx, canonical orders); new `kimatta-application` crate `cover_cycle` (assess → plan → propose → ledger; `apply` under `NeedsAttention` returns `applied=false`); bridge `api/planner.rs` one `cover_cycle` command with nested DTOs, `KimattaError::Planner` + Dart `describeFailure` arm; codegen diff generated-only. **Ordering/beam rationale:** canonical `(date,slot)`; B=8, K=12, horizon 2 days — recorded in the card's decision gates with the 588/1,932/8,844 state math (`states_scored_equals_the_per_slot_sum_from_the_trace` pins 1,932); MVP-025 may revise. **AC evidence:** AC-1 PASS (`tier0_rejections_are_never_selected_at_beam_widths_1_and_64` B∈{1,64}×K∈{1,12} incl. lock+veto; `apply_never_touches_a_locked_slot`); AC-2 PASS (`beam_is_deterministic_across_repeated_runs_and_input_permutations`, `repeat_runs_and_reopen_produce_identical_results_and_hashes` via tempfile, ledger row carries version/hash/codes); AC-3 PASS (`sequence_fixture_picks_cycle_optimal_over_slot_optimal`, property not exact-plan); AC-4 PASS (four §10 tests + `all_four_satisfied_yields_covered` + `issue_text_table_never_claims_safety_exact_stock_or_personalisation` regex over the one `ISSUE_TEXT` table and emitted strings); AC-5 PASS (`attention_requests_carry_every_field`; `planner_source_has_no_io_imports` greps `std::fs|std::net|std::time|rusqlite|reqwest` over `src/planner`); AC-6 PASS (`ledger_update_is_refused`/`ledger_delete_is_refused` at the engine, `historical_ledger_rows_are_byte_unchanged_after_a_later_run` via `SELECT *` before/after replan over changed input, `deleting_a_household_with_ledger_rows_is_refused` unconditional); AC-7 PASS (`cargo fmt --all --check` rc 0; `cargo clippy --workspace --all-targets -- -D warnings` rc 0; `grep -rn unsafe` = forbid lines + pre-existing generated `frb_generated.rs` only; `cargo tree -p household-core` = thiserror); AC-8 PASS (one `cover_cycle` command; `git diff --stat` on `frb_generated.rs`/`lib/src/rust` generated-only; Dart `a cover cycle outcome crosses the bridge`). **Gates, all rc 0, single invocations:** `cargo test --workspace` **538** (food-domain 199, household-core 8, application 7, storage 233, bridge 91; Step 0 baseline 464); `cargo build --release` then `flutter test` **287** (Step 0 baseline **286**, not the plan's stated 278; +1 native cover-cycle test, v9 pin renamed to v10); `flutter analyze` (No issues found); `dart format --output=none --set-exit-if-changed .`; `flutter build apk --debug`; invariant 17 `grep -rn "sqlite\|sqflite" lib/` empty. **Recorded deviations from the plan:** (1) T4 term `FALLBACK_PLACEHOLDER −1` on the generated FrozenQuick candidate — without it a signal-less household loses the text tie-break to the fallback (found by the Dart bridge test; pinned by `a_recipe_beats_the_frozen_quick_fallback_on_a_signal_less_household`); (2) `StorageError` gained `Kernel(#[from] KernelError)` and `CorruptLedgerEntry` — corrupt stored status/source tokens are reported, never coerced (`a_corrupt_status_token_is_reported_not_coerced`); (3) `ApplicationError::Storage` maps back to `KimattaError::Storage` so a ghost household reads identically on every screen; `KimattaError::Planner` exists with its Dart arm but is reserved; (4) a locked meal a Tier-0 check would reject stays `LockedByUser` with `LOCK_HELD_OVER:<code>` recorded (a lock is the user's own Tier-0 word); (5) leftovers coverage: sourceless leftovers never count as a meal for the §2.2 `needs_attention` trigger. **On-device: NOT RUN (optional)** — no UI until MVP-024; per the card's evidence plan. **Next implementation task: none** — the orchestrator promotes `MVP-024` (or per SEQUENCE) at `Done`. Not committed or pushed. |
| 2026-09-02 | MVP-023 | Done | Orchestrated approval under `# orchestrate: auto-approve`: review `redteam-impl-handoff-orch-33-2026-09-02T1158-e249.md`, `redteam-impl-handoff-orch-33-2026-09-02T1317-9799.md`, `redteam-impl-handoff-orch-33-2026-09-02T1401-4b38.md`, `redteam-impl-handoff-orch-33-2026-09-02T1502-ac00.md`, `redteam-impl-handoff-orch-33-2026-09-02T1548-e7cc.md`; 1 fix round(s); independent verifier `PASS` (session `a8beeb19-a5e6-4cd1-8a29-99f7a720ac08`); merged into `integration` as `56dcd05140e1280da3daf38f159a7d73d6d4b763`. Owner ratifies at the `integration → master` merge. |
| 2026-09-02 | MVP-025 | Verify | Plan `read-docs-prd-v3-md-docs-household-contr-curious-ember.md`, executed 2026-09-02. **AC-1 PASS** — 11 version-controlled synthetic fixtures (`rust/crates/food-domain/src/planner/fixtures/mod.rs`, feature-gated builder module `fixtures = []`, no serde; PRD §18's nine + the pivot prompt's two), each documented with stressor and source in `fixtures/README.md`; determinism pinned by `fixtures_are_deterministic` plus per-fixture `snapshot_hash` literals (`fixture_hashes_are_pinned`), declared stressors asserted structurally (`fixture_shapes_hold`), README coverage by `readme_lists_every_fixture`; each fixture carries its full `Recipe`s projected through `candidate_info_of` (field-for-field the storage loader's projection), keeping the set reusable for PRD §22's MILP/CP-SAT comparison. **AC-2 PASS** — six invariant tests, each demonstrated red against a deliberately broken planner and green after revert: invariants 1–3 in `planner/invariant_tests.rs` quantified over all 11 fixtures × (B,K) ∈ {(1,1),(8,12),(64,64)}, plus invariant 4 over all 11 fixtures (its universe-unchanged and per-slot feasible-subset checks call `candidates::generate`/`tier0::filter` directly and are parameter-independent; its proposal corollary runs at (1,1) and (8,12)) (`invariant_1_a_known_hard_restriction_is_never_selected_on_any_fixture`; `invariant_2_a_locked_meal_never_moves_on_any_fixture`; `invariant_3_same_input_and_version_yield_identical_results_on_every_fixture` — full-struct + `canonical_text()` byte identity across repeated runs, permutation robustness staying with MVP-023's existing test; `invariant_4_adding_a_hard_constraint_never_makes_an_infeasible_candidate_feasible` — universe-unchanged + per-slot feasible-subset over a catalog of added Known/Other restrictions, per-token hard vetoes and tightened windows, locks excluded by design per tier0's `LOCK_HELD_OVER` contract); invariant 5 twice (`invariant_5_shopping_quantities_are_well_formed_and_pantry_never_subtracts` domain-level; `invariant_5_shopping_is_well_formed_end_to_end` storage e2e) and invariant 6 (`invariant_6_a_second_apply_never_mutates_the_first_ledger_rows` — byte-for-byte cycle-1 ledger+meal rows across a second plan/apply, ledger growth, trigger refusal) in `kimatta-storage/src/controller.rs`. Red-demo catalog (per-invariant sabotage file:line → observed failing assertion) version-controlled in `fixtures/README.md` "Red demonstrations"; sabotages in brief: tier0 conflict→assumption (inv 1: peanut `r-lk-1` proposed), tier0 `LOCK_CONFLICT` deleted (inv 2: locked 08-30 dinner became `Leftovers`), beam tie-break through a process-global `AtomicUsize` (inv 3: cold_start runs diverged deterministically), restriction check gated on `hard_vetoes.is_empty()` (inv 4: `veto:beans` made `r-lk-1` feasible), pantry-marked quantity halved (inv 5: cheese `4/1`→`2/1`), `controller_ledger_no_update` trigger neutered `WHEN 0` in its migration (inv 6: direct `UPDATE` succeeded). The fix round added two further rows for assertions it strengthened, each a two-state demonstration (same sabotage, pre-fix green / post-fix red): row 1b — `candidates.rs:366` drifts the *slug* while keeping `STARTER_NOTE_PREFIX` intact, so the stub is still recognised but no longer resolves (`cold_start: proposal names unknown starter slug "fx-lentil-stew-drift"`, where the pre-fix `unwrap_or_default()` passed silently); row 5b — `shopping.rs:508` `marked: Default::default()` makes the derivation ignore `pantry_marked` outright (`marking every identity must flip at least one line to OmittedPantryMarked`, which the pre-fix test could not catch because `marked` was then bit-identical to `unmarked`). No sabotage committed; `git status` clean after all eight. **AC-3 PASS** — sweep of every new assertion: all invariant-form (non-membership, subset, byte-identity of paired runs/derivations, locked-slot equality where the lock admits exactly one answer); no exact-plan assertion over a tie-admitting fixture. **AC-4 PARTIAL** — benchmark `food-domain/examples/beam_width.rs` (`[[example]] name="beam_width" required-features=["fixtures"]`; reproduce: `cargo run -p food-domain --release --features fixtures --example beam_width`). Method: verdict cells (8,12) and (64, K=per-fixture feasible cap) computed on all 11 fixtures; exploratory grid B∈{1,2,4,8,16,32,64}×K∈{2,4,6,8,12,16,24} on the five search-heavy fixtures (`high_variety_household`, `busy_week`, `repetitive_meal_library`, `leftovers_fallback_heavy`, `multiple_strong_dislikes`; the selection criterion is recorded in the example's `SEARCH_HEAVY` doc comment, and the module doc records the monotonicity argument that makes a subset sweep verdict-safe — `beam.rs`'s `keep = min(len, per_slot)` and its truncated total order give `ranked_K ⊆ ranked_K'`, so the `(64, feasible cap)` cell, computed for **all eleven** fixtures, upper-bounds every grid cell for its fixture); per cell the hardware-independent `states_scored` and lexicographic `Score([i64;6])`, plus median wall of 5 runs after 1 warmup as host context. Pre-stated rule: shipped B=8/K=12 survives unless some computed cell is strictly lexicographically better on any fixture; first differing index 0–2 adverse (MVP-023 handback), 3–5 recorded headroom. **Verdict: B=8/K=12 SURVIVES** — no strictly-better cell at indices 0–2 on any fixture. 23 headroom cells recorded, all on `multiple_strong_dislikes` and all reaching the same `[7,0,6,0,-1,-3]` vs shipped `[7,0,6,0,-2,-4]`, first difference at index 4 (attention tier, +1). With that fixture now in the exploratory grid the headroom resolves to a **beam-width** effect rather than a candidate-limit one: every cell with B≥16 reaches it at every swept K (K∈{2,…,24}), as does (B=8, K=2); no cell with B≤4 does, and B=8 does not at K≥4. B=16 sits inside the bridge's 1..=16 clamp, so the headroom is reachable without a contract change if a later card decides to take it. Per the pre-stated rule a first difference at indices 3–5 is measured headroom, not an adverse result: **no MVP-023 handback**. Hardware: **host linux/x86_64, 13th Gen Intel(R) Core(TM) i7-13700K — not a device budget** (e.g. high_variety_household: (8,12) 1,356 states / ~51 ms host; (64,51) 41,718 states / ~1.9 s host — `states_scored` unchanged from the pre-fix run, as expected since no fix touches that fixture). The card's "device model" evidence element is `NOT VERIFIED`, D-027-shaped: (1) the resolving action — running the benchmark on a representative Android device — is owner-operated hardware outside this card's agent-executable scope (card External actions: None; `adb devices` empty on this host); (2) discharged at `MVP-022`; owner-runnable path recorded: `cargo ndk -t arm64-v8a build -p food-domain --release --features fixtures --example beam_width` (the `aarch64-linux-android` target is installed), `adb push rust/target/aarch64-linux-android/release/examples/beam_width /data/local/tmp/`, `adb shell /data/local/tmp/beam_width`; (3) owner acceptance of the residual risk is pending — this row records the exception, not its acceptance, so the card stops at `Verify`. **AC-5 PASS** — `cargo test --workspace` from `rust/`: 587 tests green (576 pre-card baseline + 11 new), no Flutter toolchain invoked; `cargo fmt --all --check`, `cargo clippy --workspace --all-targets -- -D warnings` and `cargo clippy -p food-domain --features fixtures --all-targets -- -D warnings` all clean; `cargo test --workspace`/`cargo build` skip the example via `required-features`. One MVP-023 pin updated: `planner_source_has_no_io_imports` file count 8→9 for the new cfg(test) `invariant_tests.rs`, which the same banned-import scan now covers. |
| 2026-09-02 | MVP-025 | Done | Orchestrated approval under `# orchestrate: auto-approve`: review `redteam-impl-handoff-orch-35-2026-09-02T1736-f8f4.md`; 1 fix round(s); independent verifier `NOT_VERIFIED` (session `7839c995-83f0-495b-b397-57ffffd9536d`); merged into `integration` as `101e6b69c5d39d9f4041a8f5cbc70ce9a0d87ee0`. Owner ratifies at the `integration → master` merge. AC-4's on-device element stands `NOT VERIFIED` under the D-027 exception, owner-accepted 2026-09-02: (1) representative Android hardware is not the card's work to procure; (2) discharged at `MVP-022` by re-running `cargo run -p food-domain --release --features fixtures --example beam_width` on a real low/mid-range device — tracked in `KNOWN_ISSUES.md`; (3) residual risk accepted: the (B, K) = (8, 12) tuning could shift on device, while the gating invariant tests are hardware-independent and PASS. The verifier independently reproduced the host benchmark (output matches this register's row). |
| 2026-09-02 | MVP-024 | Verify | Plan `read-docs-prd-v3-md-docs-household-contr-glimmering-scroll.md`, executed 2026-09-02 (two sessions; first hit its budget mid-step-9). **Delivered:** `food.restrictions_reviewed` policy type (conditional canonical-text line keeps all 11 fixture hash pins byte-identical; `canonical_text_covers_every_field` 17→18); both `RESTRICTIONS_NOT_CONFIGURED` emission sites guarded (reviewed+empty suppresses, §10); `record_decision` in `kimatta-application` (Swap writes locked/User, typed `LockedPlannedMeal` rollback; Veto = `food.hard_veto`; RestrictionsReviewed; one IMMEDIATE tx, `selected_action="correct"`, payload `corrects_seq=<household max seq>`); new storage `_in` helpers; bridge `record_plan_decision` (`rust/src/api/decisions.rs`, dataful FRB enum, codegen clean) and ±520 `offset_cycles` bound (`MAX_OFFSET_CYCLES`, typed `Planning` pre-storage); Dart `cover_copy`/`cover_provider`/`cover_screen` + router (UI never reads `result.reason_codes`; assumptions disclosure from the `assumptions` code list, `PANTRY_INCOMPLETE` unmapped-quiet; option-tap swap restricted to anchored full-match `recipe:` tokens). **Evidence:** AC-1 PASS (widget preview test; native `cover→swap→apply` preserves slots; emulator offline invocation verified — network disabled, one tap produced the result view, `docs/measurements/MVP-024_AC1_offline_cover.png` — but the emulator half showing a *coherent plan* is NOT VERIFIED: the fresh household had no recipes, UI-automated recipe seeding did not land in-session; the coherent-plan half is carried by the widget/native tests; owner can discharge by one manual run after adding a recipe). AC-2 PASS (locked-tile widget test + native locked-slot preservation + `swap_onto_a_locked_slot_fails_typed_and_writes_nothing`). AC-3 PASS (paired widget fakes: 2 material questions vs zero on low-value-only; engine-level urgency-scoped pair `low_value_uncertainty_raises_no_high_urgency_request_but_material_does`). AC-4 PASS (Accept 1 tap, Swap 2 taps, veto confirm→record, all three written as `correct` ledger rows; native `ledgerEntryId` non-empty per decision). AC-5 PASS (accept→invalidate cascade→Shopping rendered with no generate step, widget test). AC-6 PASS (repo grep: no streak/badge/feed/notification/gamification hits in `lib/`; AndroidManifest has zero permission entries). AC-7 PASS (guideline trio both brightnesses + textScale 2.0 widget tests; emulator screenshot `docs/measurements/MVP-024_AC7_textscale2_cover.png`). AC-8 **NOT VERIFIED** — protocol, scenarios, counterbalancing and record template in `docs/measurements/MVP-024_AC8_ATTENTION.md`; D-027 route: (1) resolving action needs a human operator with a stopwatch, outside orchestrated scope; (2) discharged by owner measurement before `MVP-022`'s §22 proof (adverse result returns `MVP-023` to Draft under D-031); (3) owner acceptance pending — card holds at `Verify`. **Gates:** cargo 602 pass, clippy clean, fmt clean, `flutter analyze` clean, `flutter test` 224+ pass, native suite 42 pass. KNOWN_ISSUES: `offset_cycles` RESOLVED; `ACTION_DECLINED` RESOLVED-as-shipped (+ new low residual); `LOCK_CONFLICT` re-deferred to first reason-code-rendering card; dead `Planner` variant marked already-fixed; whole-window-at-offset-0 decision noted (MVP-013 open note closed). **Amendment 2026-09-02 (redteam remediation, same card):** the "typed `LockedPlannedMeal` rollback" above no longer describes the shipped code. `LockedPlannedMeal` is the *automation* refusal (both storage raise sites gate on `WriteSource::Automation`), so a `WriteSource::User` swap borrowing it put a raw meal id and the words "locked against automation" in a household-facing snackbar; the swap arm now returns `ApplicationError::SwapOntoLockedSlot { meal, date, slot }`, which the bridge maps to the prose "that meal is locked, so it cannot be swapped". AC-2's evidence test `swap_onto_a_locked_slot_fails_typed_and_writes_nothing` is unchanged and still passes -- only the variant it matches changed -- and the question card now gates its Swap affordance on the addressed slot's lock (`a question on a locked slot offers no swap, and says why`). Also in the same pass: `ApplicationError::BlankVetoSubject` -> `UnmatchableVetoSubject`, whose guard is now the matcher's own `food_domain::is_vetoable` rather than `trim().is_empty()`, so a subject that tokenises to nothing is refused instead of stored as an inert irreversible rule. Gates re-run, all rc 0: `cargo test --workspace` **615**, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo fmt --all --check`, `cargo build --release`, `flutter test` **316**, `flutter analyze`, `dart format --set-exit-if-changed`. |
| 2026-09-02 | MVP-024 | Done | Orchestrated approval under `# orchestrate: auto-approve`: review `redteam-impl-handoff-orch-37-2026-09-02T1949-115e.md`, `redteam-impl-handoff-orch-37-2026-09-02T2041-c699.md`, `redteam-impl-handoff-orch-37-2026-09-02T2127-eb06.md`; 0 fix round(s); independent verifier `NOT_VERIFIED` (session `99015ab5-cc7b-40f1-ad9c-274ecd82ffd3`); merged into `integration` as `52b6134f9d93539088dce2f5b732adfb94f936db`. Owner ratifies at the `integration → master` merge. AC-8 stands `NOT VERIFIED` under the D-027 exception, owner-accepted 2026-09-02: (1) the attention measurement requires a human operator with a stopwatch — not the card's own work; (2) discharged by the owner running `docs/measurements/MVP-024_AC8_ATTENTION.md` **before DEC-003** (tightened from the protocol's before-MVP-022 bound) — tracked in `KNOWN_ISSUES.md`; (3) residual risk accepted: an adverse result remains a hard stop returning `MVP-023` to Draft under D-031 — approval here does not soften it. Verifier evidence: 7/8 ACs PASS on its own observation, including an emulator drive-through of AC-1. |
| 2026-09-03 | MVP-017 | Done | Orchestrated approval under `# orchestrate: auto-approve`: review `redteam-impl-handoff-orch-39-2026-09-02T2320-5b75.md`, `redteam-impl-handoff-orch-39-2026-09-03T0012-89d3.md`; 2 fix round(s); independent verifier `PASS` (session `c231762a-f0f6-4904-989a-ceff0ce8907f`); merged into `integration` as `0888a9f59f39a740d57f8054663f39112c7f0df9`. Owner ratifies at the `integration → master` merge. |
