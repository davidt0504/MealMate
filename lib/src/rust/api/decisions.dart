// This file is automatically generated, so please do not edit it.
// @generated by `flutter_rust_bridge`@ 2.13.0.

// ignore_for_file: invalid_use_of_internal_member, unused_import, unnecessary_import

import '../frb_generated.dart';
import 'error.dart';

import 'package:flutter_rust_bridge/flutter_rust_bridge_for_generated.dart';
import 'package:freezed_annotation/freezed_annotation.dart' hide protected;

import 'planned_meals.dart';
import 'planner.dart';
import 'planning.dart';
part 'decisions.freezed.dart';

// These functions are ignored because they are not marked as `pub`: `record_in`
// These function are ignored because they are on traits that is not defined in current crate (put an empty `#[frb]` on it to unignore): `assert_fields_are_eq`, `assert_fields_are_eq`, `assert_fields_are_eq`, `clone`, `clone`, `clone`, `eq`, `eq`, `eq`, `fmt`, `fmt`, `fmt`

/// Records one decision: mutation, re-assessment and the `correct` ledger row in one
/// IMMEDIATE transaction.
Future<PlanDecisionOutcomeDto> recordPlanDecision({
  required PlanDecisionRequestDto request,
}) =>
    RustLib.instance.api.crateApiDecisionsRecordPlanDecision(request: request);

@freezed
sealed class PlanDecisionDto with _$PlanDecisionDto {
  const PlanDecisionDto._();

  const factory PlanDecisionDto.swap({
    /// ISO civil date.
    required String date,
    required MealSlotDto slot,
    required List<MealComponentDto> components,
  }) = PlanDecisionDto_Swap;
  const factory PlanDecisionDto.veto({
    required String subject,

    /// ISO civil date of the slot the veto was uttered on, for the ledger only.
    required String date,
    required MealSlotDto slot,
  }) = PlanDecisionDto_Veto;
  const factory PlanDecisionDto.restrictionsReviewed() =
      PlanDecisionDto_RestrictionsReviewed;
}

class PlanDecisionOutcomeDto {
  final String ledgerEntryId;
  final OutcomeStatusDto priorStatus;
  final OutcomeStatusDto resultingStatus;

  const PlanDecisionOutcomeDto({
    required this.ledgerEntryId,
    required this.priorStatus,
    required this.resultingStatus,
  });

  @override
  int get hashCode =>
      ledgerEntryId.hashCode ^ priorStatus.hashCode ^ resultingStatus.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is PlanDecisionOutcomeDto &&
          runtimeType == other.runtimeType &&
          ledgerEntryId == other.ledgerEntryId &&
          priorStatus == other.priorStatus &&
          resultingStatus == other.resultingStatus;
}

/// Carries the snapshot inputs explicitly: recording a decision re-assesses the same window
/// `cover_cycle` planned, so it needs the same `(today, offset_cycles)` addressing.
class PlanDecisionRequestDto {
  final String householdId;

  /// ISO civil date.
  final String today;

  /// 0 is the active window, as `planning_cycle_window` counts.
  final int offsetCycles;
  final PlanDecisionDto decision;

  const PlanDecisionRequestDto({
    required this.householdId,
    required this.today,
    required this.offsetCycles,
    required this.decision,
  });

  @override
  int get hashCode =>
      householdId.hashCode ^
      today.hashCode ^
      offsetCycles.hashCode ^
      decision.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is PlanDecisionRequestDto &&
          runtimeType == other.runtimeType &&
          householdId == other.householdId &&
          today == other.today &&
          offsetCycles == other.offsetCycles &&
          decision == other.decision;
}
